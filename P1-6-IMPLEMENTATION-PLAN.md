# P1-6 — Implementation Plan (مقترح فقط، لم يُنفَّذ شيء)

**هذه خطة تنفيذ للمناقشة والموافقة فقط. لم يُطبَّق أي migration، لم يُنفَّذ أي `CREATE/ALTER/
UPDATE/DELETE/INSERT`، ولم يُعدَّل أي ملف كود بعد. كل ما هو أدناه SQL/JS مقترَح — نصّ جاهز
للتنفيذ لاحقًا فقط بعد موافقتك الصريحة.**

مبنية مباشرة على النتائج الموثَّقة في `P1-6-AUDIT-REPORT.md` (القسم 10 خصوصًا)، وتغلق فقط
الفجوتين المحدَّدتين هناك:
1. غياب طبقة تحقق على مستوى القاعدة لـ `resources.file_url`.
2. حالة الفراغ/whitespace الحدّية في `safeResourceUrl()`.

---

## 1. التغيير الدقيق المقترح

### 1.1 — طبقة القاعدة: `CHECK constraint` جديد على `resources.file_url`

قيد إضافي واحد يفرض بالضبط نفس السياسة المطبَّقة فعليًا اليوم في `isValidResourceUrl()`
(`admin/admin.js`) و`safeResourceUrl()` (`js/app.js`) — **مخطّط http/https فقط، وغير فارغ بعد
إزالة المسافات الطرفية** — بحيث تصبح هذه السياسة مفروضة أيضًا من القاعدة نفسها لأي مسار كتابة
مستقبلي يتجاوز واجهة الأدمن (نداء PostgREST مباشر مثلًا)، لا فقط من الواجهة.

**لا تغيير في المعنى المسموح به اليوم عبر الواجهة** — القيد يطابق تمامًا ما يقبله
`isValidResourceUrl()` حاليًا (http/https)، فلا يرفض أي قيمة كانت الواجهة تقبلها من قبل.

### 1.2 — طبقة العرض: معالجة صريحة لحالة الفراغ في `safeResourceUrl()`

إضافة فحص مبكر: إن كانت القيمة فارغة أو whitespace بعد `trim`، إرجاع `"#"` **فورًا** دون تمرير
القيمة لـ `new URL(url, window.location.href)` — يمنع بالضبط الخلل الوظيفي الموثَّق في §8 من
تقرير الـ Audit (رابط "فارغ" يتحول حاليًا لرابط ذاتي المرجع بدل `"#"`).

**لا تغيير في سلوك أي قيمة أخرى** — `javascript:`/`data:`/URL صالح/URL غير قابل للتحليل: كلها
تسلك نفس المسار (try/catch الحالي) كما هي تمامًا، دون أي تعديل على منطقها.

### 1.3 — ما **لا** يتغيّر (تأكيد صريح على الالتزام بالقيود المطلوبة)

- `isValidResourceUrl()` (`admin/admin.js`): **بلا أي تعديل** — تبقى كما هي، القيد الجديد في
  القاعدة يوازيها، لا يستبدلها ولا يتجاوزها.
- `safeResourceUrl()`: تعديل **إضافي واحد فقط** (فحص مبكر للفراغ) — بقية الدالة (فحص المخطّط عبر
  `new URL`) **بلا أي تغيير**.
- MFA، Session Lock/P1-7B، RLS، الصلاحيات، Storage architecture: **صفر لمسات** — لا علاقة لأي
  منها بعمود `file_url` (مؤكَّد في تقرير الـ Audit §9)، ولن يُلمس أي منها في هذا التنفيذ المقترح.
- بيانات الإنتاج: **صفر تعديل** — القيد الجديد `CHECK` فقط، لا `UPDATE` على أي صف. (والقاعدة
  الحية أصلًا فارغة من الموارد — لا صفوف موجودة لتنتهك القيد الجديد على أي حال، راجع §4).

---

## 2. الملفات التي ستتأثر

| الملف | نوع التغيير |
|---|---|
| ملف SQL migration **جديد** (سيُسمَّى مثلًا `sql/phase4d_p1_6_file_url_check.sql`) | **جديد بالكامل** — `ALTER TABLE ... ADD CONSTRAINT ...` فقط، إضافي، بلا `DROP`/`ALTER COLUMN` |
| `js/app.js` | **تعديل بسطرين** داخل `safeResourceUrl()` فقط (§1.2) |
| `admin/admin.js` | **بلا أي تعديل** |
| `admin/index.html` | **بلا أي تعديل** |
| أي ملف SQL/RLS/RPC موجود | **بلا أي تعديل** |

لا ملفات أخرى في النطاق.

---

## 3. SQL المقترح (نص جاهز — لم يُطبَّق)

```sql
-- ============================================================
-- Phase 4D — P1-6: قيد قاعدة بيانات على resources.file_url
-- ============================================================
--
-- يفرض على مستوى القاعدة بالضبط نفس السياسة المطبَّقة فعليًا اليوم من
-- طرف العميل في isValidResourceUrl() (admin/admin.js) وsafeResourceUrl()
-- (js/app.js): مخطّط http/https فقط، وغير فارغ بعد إزالة المسافات
-- الطرفية. لا يغيّر أي سلوك مقبول حاليًا عبر واجهة الأدمن — فقط يمنع
-- عبور القيم المرفوضة أصلًا من أي مسار كتابة يتجاوز تلك الواجهة.
--
-- التسمية تتبع نمط بقية قيود الجدول (resources_<column>_check) كما هو
-- مستخدَم فعليًا لـ type/language/source_type/status/storage_provider.
-- ============================================================

alter table public.resources
  add constraint resources_file_url_check
  check (
    btrim(file_url) <> ''
    and btrim(file_url) ~* '^https?://\S+'
  );

comment on constraint resources_file_url_check on public.resources is
  'P1-6: يفرض أن يكون file_url رابطًا مطلقًا بمخطّط http/https غير فارغ — يطابق isValidResourceUrl() (admin/admin.js) وsafeResourceUrl() (js/app.js) دون تغيير سلوكهما.';
```

**شرح الـ regex** (`^https?://\S+`، بعد `btrim`):
- `^https?://` — يطابق `http://` أو `https://` في بداية القيمة (بعد إزالة المسافات الطرفية) —
  نفس فحص `parsed.protocol === "http:" || "https:"` الحالي، بصيغة نصية مكافئة.
- `\S+` — يشترط وجود حرف واحد غير فراغ على الأقل بعد المخطّط (يمنع `"https://"` وحدها بلا مسار،
  وأي قيمة تحتوي مسافات داخلية بعد المخطّط مباشرة).
- `btrim(file_url) <> ''` — يمنع الفراغ والـ whitespace-only صراحة (هذا بالضبط ما كان يعبر
  القاعدة سابقًا).
- **لا** تحليل URL كامل داخل القاعدة (Postgres لا يملك دالة `URL` مكافئة لـ JavaScript) — هذا
  متعمَّد ومتوافق مع تعليق الكود الموجود في `admin.js` نفسه، الذي يوضّح أن التحليل الكامل
  (نسبي/مطلق، معالجة استثناءات التحليل) يبقى مسؤولية طبقة JavaScript؛ القيد هنا "شبكة أمان" على
  مستوى النمط النصي فقط، وليس بديلًا عن `isValidResourceUrl()`.

**Rollback جاهز (للتوثيق فقط، غير مُفعَّل الآن)**:
```sql
alter table public.resources drop constraint resources_file_url_check;
```

---

## 4. تأثيره على البيانات الموجودة

- **القاعدة الحية فارغة تمامًا من الموارد اليوم** (0 صف في `resources`، مؤكَّد في تقرير الـ
  Audit §7) — إضافة القيد ستنجح **فورًا وبأمان تام**، لأن Postgres يتحقق من القيد الجديد ضد كل
  صف موجود عند `ADD CONSTRAINT`، ولا صفوف موجودة لتنتهكه.
- **لا يوجد أي `UPDATE`/تعديل على أي بيانات** في هذا المقترح — القيد فقط يمنع صفوفًا **مستقبلية**
  غير صالحة من الدخول، لا يلمس أي صف حالي.
- إن أُضيفت موارد لاحقًا (بعد هذا القيد) عبر واجهة الأدمن الحالية: **لا تأثير مطلقًا** — القيد
  يطابق تمامًا ما تقبله `isValidResourceUrl()` أصلًا، فلن يُرفض أي إدخال كان سيُقبل سابقًا.
- إن حاول أي مسار (اختبار أو استخدام مستقبلي) إدخال `file_url` فارغ أو بمخطّط غير http/https
  **عبر PostgREST مباشرة متجاوزًا الواجهة**: سيُرفض الآن من القاعدة برسالة خطأ قياسية لانتهاك
  `CHECK constraint` (بدل أن يُقبل بصمت كما هو الحال اليوم) — وهذا بالضبط الهدف من §10.1 في تقرير
  الـ Audit.

---

## 5. اختبارات القبول (Acceptance Tests)

**تنفَّذ لاحقًا فقط بعد التطبيق الفعلي، وبعد الموافقة — وستُنفَّذ داخل `BEGIN...ROLLBACK` (كما في
كل الاختبارات السابقة لهذا المشروع) لعدم ترك أي بيانات اختبار، تمامًا كما يقتضي القيد المطروح في
طلبك.** القائمة أدناه هي خطة الاختبار، وليست نتائج فعلية بعد.

| # | الحالة | النتيجة المتوقَّعة بعد التطبيق |
|---|---|---|
| T1 | `INSERT` بـ `file_url = 'https://example.com/file.pdf'` | ينجح (سلوك حالي محفوظ) |
| T2 | `INSERT` بـ `file_url = 'http://example.com/file.pdf'` | ينجح (سلوك حالي محفوظ — المشروع لا يفرض HTTPS حصرًا، لم يتغيّر هذا) |
| T3 | `INSERT` بـ `file_url = ''` | **يُرفض** الآن بخطأ `CHECK constraint` (كان يمر سابقًا) |
| T4 | `INSERT` بـ `file_url = '   '` (whitespace فقط) | **يُرفض** الآن (كان يمر سابقًا) |
| T5 | `INSERT` بـ `file_url = 'javascript:alert(1)'` | يُرفض (كان مرفوضًا أصلًا عبر الواجهة، الآن مرفوض أيضًا من القاعدة مباشرة) |
| T6 | `INSERT` بـ `file_url = 'data:text/html,...'` | يُرفض (نفس T5) |
| T7 | `INSERT` بـ `file_url = 'not-a-url'` | يُرفض |
| T8 | `INSERT` بـ `file_url = '  https://example.com/file.pdf  '` (مسافات طرفية حول رابط صالح) | ينجح (يُقبل بعد `btrim` ضمنيًا في الفحص، متوافق مع أن `admin.js` يرسل القيمة بعد `.trim()` أصلًا) |
| T9 | تحديث (`UPDATE`) مورد موجود بتغيير `file_url` فقط إلى قيمة غير صالحة | يُرفض بنفس القيد (القيد يُفرض على `INSERT` و`UPDATE` معًا تلقائيًا في Postgres) |
| T10 | إعادة تشغيل مسار الإضافة/التعديل الكامل عبر واجهة الأدمن الحالية (`isValidResourceUrl` ثم `insert`/`update`) بقيمة صالحة | ينجح دون أي تغيير محسوس من طرف المستخدم — تأكيد أن القيد الجديد "شفّاف" تمامًا للمسار الطبيعي |
| T11 (JS، بدون قاعدة) | استدعاء `safeResourceUrl('')` و`safeResourceUrl('   ')` مباشرة | يُرجعان `"#"` فورًا (بدل الرابط الذاتي المضلِّل الحالي) |
| T12 (JS، بدون قاعدة) | استدعاء `safeResourceUrl('javascript:x')`, `safeResourceUrl('data:x')`, `safeResourceUrl('https://valid.com')` | **بلا أي تغيير** في النتيجة عن السلوك الحالي — تأكيد أن التعديل لم يمسّ المنطق الموجود |
| T13 | فحص حي بعد التطبيق: `pg_get_constraintdef` على القيد الجديد + قائمة كل قيود `resources` الأخرى | القيد الجديد موجود بالتعريف المتوقَّع؛ القيود الست الأخرى (`type`/`language`/`source_type`/`status`/`storage_provider`/`pkey`/`fkey`) **بلا أي تغيير** |
| T14 | فحص عدم وجود Regression: RLS policies على `resources`، `search_resources()`، `increment_resource_view()`، MFA، Session Lock | **بلا أي تغيير** (تحقّق حي `pg_get_functiondef`/`pg_policies` كما في التقارير السابقة) |

---

## 6. المخاطر وحالات Edge Cases

| الخطر/الحالة | التقييم |
|---|---|
| القيد الجديد يرفض بيانات موجودة عند التطبيق | **لا خطر فعليًا** — القاعدة الحية فارغة من الموارد اليوم (§4). لو أُضيفت بيانات بين الآن وموعد التطبيق الفعلي، يلزم فحص مسبق (`SELECT` بسيط للتأكد من عدم وجود صفوف منتهِكة) قبل `ADD CONSTRAINT` — سأنفّذ هذا الفحص وقت التنفيذ الفعلي وليس الآن. |
| القيد يرفض رابطًا كان يُقبَل سابقًا عبر واجهة الأدمن | **غير متوقَّع** — الـ regex مصمَّم ليطابق تمامًا شرط `isValidResourceUrl()` (فحص المخطّط فقط)، لكن هذا **افتراض يحتاج تأكيدًا بالاختبار الفعلي (T1/T2/T8)** قبل الاعتماد النهائي، لأن `new URL()` في JS و`~*` regex في SQL ليسا نفس محرك التحليل حرفيًا — حالات نادرة جدًا (مثل مخطّطات بأحرف كبيرة `HTTPS://`) قد تتصرف بفروقات دقيقة؛ الـ regex هنا يستخدم `~*` (غير حساس لحالة الأحرف) لتفادي هذا تحديدًا. |
| رابط صالح يحتوي مسافة **داخل** المسار (نادر لكن ممكن نظريًا في بعض الروابط غير المُرمَّزة URL-encoded) | القيد الحالي المقترح (`\S+` بعد المخطّط مباشرة فقط، لا فحص شامل لبقية الرابط) **لا يرفض** هذه الحالة إن وُجدت مسافة لاحقًا في الرابط بعد أول حرف — القيد يتحقق فقط من عدم الفراغ الكامل وصحة المخطّط، تمامًا مثل نطاق ما تتحقق منه الواجهة اليوم (لا توسيع في الصرامة). هذا اتساق متعمَّد مع النطاق المطلوب، وليس نقصًا. |
| تعارض مع أي مسار كتابة آخر غير معروف لي حاليًا | راجعت كل مسارات الكتابة الموجودة فعليًا في المستودع (نموذج الأدمن فقط — لا يوجد مسار آخر) في تقرير الـ Audit. لا يوجد خطر معروف، لكن أي دالة/سكربت مستقبلي يكتب `resources` مباشرة (خارج هذا المستودع، مثل سكربت استيراد بيانات) سيحتاج الالتزام بنفس الشرط من الآن فصاعدًا — وهذا **الهدف المقصود** من القيد، لا خطر جانبي. |
| فحص `safeResourceUrl('')` قد يُستخدَم بافتراض ضمني آخر في مكان لم أكتشفه | راجعت في تقرير الـ Audit كل نقطة استخدام لـ`safeResourceUrl` — نقطة واحدة فقط (`buildResourceCard` في `js/app.js:198`). لا استخدام آخر، فالتعديل معزول تمامًا الأثر. |
| اسم القيد `resources_file_url_check` يتعارض مع قيد موجود بنفس الاسم | **لا يوجد** — تحقّقت حيًا (§قائمة القيود أعلاه في هذه الجلسة) أن هذا الاسم غير مستخدَم حاليًا على الجدول. |

---

## الخلاصة

هذه الخطة تغلق الفجوتين المحدَّدتين في تقرير الـ Audit **بأقل تغيير ممكن**: قيد `CHECK` واحد
إضافي على القاعدة (بلا أي `migration` يمسّ أي شيء آخر)، وتعديل سطرين في دالة موجودة أصلًا
(`safeResourceUrl`) دون تغيير منطقها الحالي. لا شيء نُفِّذ بعد — بانتظار موافقتك للمتابعة إلى
التنفيذ الفعلي.
