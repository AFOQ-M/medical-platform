# تقرير — P1-6: تنفيذ قيد `resources_file_url_check` + إصلاح فراغ `safeResourceUrl()`

## الحالة النهائية: **PASS WITH LIMITATIONS**

(السبب موضَّح بالكامل في §7 — حد واحد حقيقي مُكتشَف أثناء الاختبار الفعلي، وليس افتراضًا، تمامًا
كما طلبت عدم افتراض نجاح T7.)

---

## 0. الفحص المسبق (قبل أي تنفيذ)

- **البيانات**: `resources_count=0`, `subjects_count=0`, `universities_count=1`, `profiles_count=4`
  — مطابقة تمامًا لحالة الـ Audit، بلا أي تغيير غير متوقَّع.
- **القيود الحالية على `resources`**: 7 قيود (`pkey`, `fkey`, و5 `CHECK` على
  `type/language/source_type/status/storage_provider`) — مطابقة تمامًا، `resources_file_url_check`
  لم يكن موجودًا بعد (كما هو متوقَّع).
- **الملفات**: `js/app.js` كان لا يزال **مطابقًا حرفيًا (نفس md5)** لنسخة الرفع الأصلية — لم يتغيّر
  منذ الـ Audit. `admin.js` يحمل فقط تعديلات P1-7B الموافَق عليها سابقًا (غير متعلقة بهذا الطلب).
- **لا يوجد أي تغيير غير متوقَّع** → تابعت التنفيذ كما هو مخطَّط، دون توقف.

---

## 1. الملفات المعدَّلة

| الملف | نوع التغيير |
|---|---|
| `sql/phase4d_p1_6_file_url_check.sql` | **جديد** — `ALTER TABLE ... ADD CONSTRAINT resources_file_url_check ...` فقط |
| `js/app.js` | **تعديل بسطر واحد فعلي** (+ تعليق) داخل `safeResourceUrl()` فقط — فحص مبكر للفراغ/whitespace |
| `admin/admin.js` | **بلا أي تعديل** (كما طُلب صراحة) |
| كل ملف آخر (RLS، RPCs، MFA، Session Lock، Storage، أي HTML) | **بلا أي تعديل** |

`diff -rq` كامل بين النسخة المرفوعة الأصلية والنسخة النهائية يُظهر فرقًا محصورًا فيما يلي فقط: 4
ملفات تقارير/خطط `.md` (توثيق)، ملفَي SQL migration جديدين بالكامل (`phase4c` من الطلب السابق
+ `phase4d` الجديد هنا)، و`admin/admin.js` (فقط تعديلات P1-7B السابقة الموافَق عليها) و`js/app.js`
(التعديل الوحيد في هذا الطلب).

---

## 2. Migration المطبَّق

اسم الـ migration: **`phase4d_p1_6_file_url_check`** — طُبِّق فعليًا على مشروع Supabase الحي
(`lzmkgfxlsynaphpofblb`) بنجاح (`success: true`)، بنفس النص الوارد في الخطة المعتمَدة حرفيًا:

```sql
alter table public.resources
  add constraint resources_file_url_check
  check (
    btrim(file_url) <> ''
    and btrim(file_url) ~* '^https?://\S+'
  );
```

**فحص ما قبل التطبيق (SELECT فقط)**: بحثت عن أي صف موجود في `resources` ينتهك هذا القيد —
**لا يوجد أي صف مخالف** (الجدول فارغ تمامًا، `resources_count = 0`)، فكان التطبيق آمنًا فوريًا.

**تحقّق حي بعد التطبيق**: القيد موجود بالضبط بالتعريف المتوقَّع (`pg_get_constraintdef`)، وكل
القيود الـ6 الأخرى على `resources` (`pkey`, `fkey`, و`type/language/source_type/status/
storage_provider CHECK`) **مطابقة حرفيًا لما كانت عليه قبل التطبيق** — لا تغيير على أي منها.

---

## 3. التعديل في `js/app.js`

داخل `safeResourceUrl()` فقط — إضافة فحص مبكر واحد:

```js
if (typeof url !== "string" || url.trim() === "") {
  return "#";
}
```

قبل استدعاء `new URL(url, window.location.href)` — بلا أي تغيير على بقية الدالة. تحقّقت
(`node --check`) أن الملف سليم صياغيًا.

---

## 4. نتائج الاختبارات T1–T14

نُفّذت اختبارات SQL (T1–T10, T13) حيًا على المشروع الفعلي داخل `BEGIN...ROLLBACK` واحدة — أنشأت
بيانات مساعدة مؤقتة (كلية/سنة/مادة اختبار) **داخل نفس المعاملة فقط** لتوفير `subject_id` صالح
للإدراج، ثم `ROLLBACK` في النهاية. تحققت بعدها حيًا أن لا شيء من ذلك بقي (`resources_count=0`,
`subjects_count=0`، والكلية المؤقتة غير موجودة إطلاقًا بالاسم) — **صفر بيانات اختبار متبقية**.

| # | الحالة | النتيجة | الحالة |
|---|---|---|---|
| T1 | `INSERT` بـ `https://example.com/file.pdf` | نجح | **PASS** |
| T2 | `INSERT` بـ `http://example.com/file.pdf` | نجح | **PASS** |
| T3 | `INSERT` بـ `''` (فارغ) | **رُفض** (`check_violation`) | **PASS** |
| T4 | `INSERT` بـ `'   '` (whitespace فقط) | **رُفض** | **PASS** |
| T5 | `INSERT` بـ `'javascript:alert(1)'` | رُفض | **PASS** |
| T6 | `INSERT` بـ `'data:text/html,...'` | رُفض | **PASS** |
| T7 | `INSERT` بـ `'not-a-url'` | رُفض (على مستوى **القاعدة**) | **PASS** — لكن انظر §7 لفارق مهم على مستوى JS |
| T8 | `INSERT` بـ `'   https://example.com/file.pdf   '` (مسافات طرفية حول رابط صالح) | نجح | **PASS** |
| T9 | `UPDATE` مورد موجود إلى `file_url` غير صالح | **رُفض** (القيد يُطبَّق على `UPDATE` تلقائيًا كما في `INSERT`) | **PASS** |
| T10 | مسار إضافة طبيعي بقيمة صالحة (محاكاة تدفّق `admin.js`) | نجح، بلا أي احتكاك | **PASS** |
| T11 | `safeResourceUrl('')` و`safeResourceUrl('   ')` (استدعاء فعلي للدالة المعدَّلة، ليس محاكاة) | كلاهما `"#"` (بدل الرابط الذاتي المضلِّل سابقًا) | **PASS** |
| T12 | `safeResourceUrl()` على `javascript:`, `data:`, رابط صالح، `null`, `undefined` | كلها كما كانت تمامًا (`"#"` أو الرابط الصحيح) — **بلا أي تغيير** في السلوك القديم | **PASS** |
| T13 | فحص حي: تعريف القيد الجديد + كل قيود `resources` الأخرى | القيد الجديد مطابق للمخطَّط؛ الـ6 الأخرى بلا أي تغيير | **PASS** |
| T14 | Regression: عدد سياسات RLS على `resources` (5)، دوال الموارد (2)، دوال القفل/MFA (4)، `RLS enabled` على كل من `resources` و`admin_session_lock` (كلاهما `true`)، وصف قفل الأدمن الحي (نفس `session_token` الحقيقي، heartbeat مستمر) | كلها مطابقة تمامًا لما كانت عليه قبل التنفيذ | **PASS** |

### اختبارات إضافية نفَّذتها بناءً على طلبك الصريح بعدم افتراض النجاح (JS مقابل SQL)

اختبرتها **فعليًا** عبر تشغيل الدالتين الحقيقيتين من الملفين المعدَّلين/غير المعدَّلين مباشرة
(Node)، وليس بمحاكاة نظرية:

| الحالة | `resources_file_url_check` (SQL) | `safeResourceUrl()` (JS، مع base) | `isValidResourceUrl()` (JS، بلا base — `admin.js`، غير معدَّلة) |
|---|---|---|---|
| `http://example.com/file.pdf` | يقبل | يقبل | يقبل |
| `https://example.com/file.pdf` | يقبل | يقبل | يقبل |
| `HTTPS://example.com/file.pdf` (حروف كبيرة) | يقبل (`~*` غير حساس لحالة الأحرف) | يقبل (`new URL` يطبِّع المخطّط لحروف صغيرة تلقائيًا) | يقبل (نفس السبب) |
| `HTTP://EXAMPLE.com/FILE.pdf` | يقبل | يقبل | يقبل |
| `'   https://.../file.pdf   '` (مسافات طرفية) | يقبل (`btrim`) | يقبل (`new URL` يزيل المسافات الطرفية تلقائيًا وفق مواصفة URL) | يقبل (نفس السبب) |
| `'https://.../file.pdf   '` (trailing فقط) | يقبل | يقبل | يقبل |
| `''` / `'   '` (فارغ/whitespace) | **يرفض** | **يرفض الآن** (بعد هذا التعديل) | يرفض (سبق أن كان يرفض) |
| `javascript:...` | يرفض | يرفض | يرفض |
| `data:...` | يرفض | يرفض | يرفض |
| `'not-a-url'` | **يرفض** | **⚠️ يقبل** — يحوّلها لرابط نسبي صالح تجاه الصفحة الحالية (`https://.../not-a-url`) | يرفض |
| `'https://'` (مخطّط بلا شيء بعده) | يرفض | يرفض | يرفض |

---

## 5. إثبات: هل T7 (malformed) ناجح فعلًا؟ — **جزئيًا فقط، وهذا موثَّق بدقة**

- **على مستوى القاعدة (SQL)**: `'not-a-url'` **مرفوضة فعليًا** بالقيد الجديد — أثبتُّه بإدراج فعلي
  داخل معاملة حية (`check_violation`)، وليس افتراضًا. ✅
- **على مستوى `admin.js` (`isValidResourceUrl`, بلا `base`)**: **مرفوضة فعليًا أيضًا** — لم يتغيّر
  هذا الملف، واختبرته مباشرة، `new URL('not-a-url')` بلا base يرمي استثناءً كما هو متوقَّع. ✅
- **على مستوى `js/app.js` (`safeResourceUrl`, **مع** `base = window.location.href`)**: **⚠️ غير
  مرفوضة** — `new URL('not-a-url', base)` يُحلّها المتصفح كمسار نسبي صالح تجاه الصفحة الحالية،
  فتُرجع رابطًا صحيحًا وظيفيًا (لكنه خاطئ المعنى: يعيد تحميل موقعك نفسه بدل فتح ملف). **هذا سلوك
  موجود مسبقًا ولم يتغيّر بتعديلي** (تعديلي اقتصر على الفراغ/whitespace فقط، تمامًا كما نص عليه
  النطاق المعتمَد) — والفرق بين الدالتين موثَّق أصلًا في تعليق `admin.js` نفسه (يشرح أن استخدام
  `base` في `safeResourceUrl` مقصود لأسباب أخرى غير هذه الحالة تحديدًا).

**لماذا هذا لا يشكّل مخاطرة عملية فعلية بعد هذا التنفيذ**: بما أن القيد الجديد على القاعدة يرفض
الآن `'not-a-url'`-type بشكل قاطع عند أي `INSERT`/`UPDATE` (مهما كان المصدر، حتى نداء API مباشر
متجاوز لـ`admin.js`)، فإنه **لم يعد ممكنًا فعليًا** إدخال مثل هذه القيمة في `resources.file_url`
بعد الآن — أي أن `safeResourceUrl()` لن تُستدعى عمليًا بهذه القيمة أبدًا من بيانات حقيقية جديدة
(الجدول فارغ اليوم أصلًا، §0). يبقى هذا **تباينًا نظريًا موثَّقًا** بين الدالتين (كان موجودًا قبل
تدخلي ولا يزال)، وليس ثغرة يفتحها هذا التنفيذ — لكنه **حد فعلي (limitation) يستحق التسجيل بدقة**
كما طلبت صراحة عدم افتراض نجاح T7 دون تحقق. لم أُصلح هذا التباين لأنه **خارج النطاق المعتمَد**
صراحة في الخطة (المسموح كان فقط: القيد الجديد + إصلاح حالة الفراغ في `safeResourceUrl`، لا أي
تعديل آخر في منطقها).

---

## 6. تأكيد الالتزام بالقيود

- **admin.js**: صفر تعديل — تحقّق `diff` مباشر.
- **MFA**: صفر لمسة — لم أفتح `js/auth.js` ولا أي دالة MFA في هذه الجلسة.
- **Session Lock / P1-7B**: صفر لمسة — تحقّقت حيًا أن قفل الأدمن الحقيقي (`session_token` نفسه،
  heartbeat مستمر) لم يتأثر، وأن دوال `acquire/refresh/release_admin_session_lock` مطابقة تمامًا.
- **RLS/الصلاحيات**: صفر تعديل — عدد سياسات `resources` (5) وباقي الجداول مطابق تمامًا؛ لم أُنشئ
  أو أُعدّل أي `GRANT`/`REVOKE`/policy.
- **Storage architecture**: لم تُلمَس — لا علاقة لها بهذا التغيير أصلًا (لا Supabase Storage
  مستخدَم في هذا المشروع، كما وثّق تقرير الـ Audit).
- **بيانات إنتاجية**: صفر تعديل — القيد الجديد `CHECK` فقط (لا `UPDATE`/`DELETE`/`INSERT` على أي
  صف حقيقي)؛ كل بيانات الاختبار كانت داخل `BEGIN...ROLLBACK` وتحقّقت من عدم بقاء أي أثر منها.
- **لا توسيع في النطاق**: لم أعدّل أي feature أو سلوك خارج `resources.file_url`.

---

## 7. القيود المتبقية (Limitations)

1. **التباين الموثَّق في §5**: `safeResourceUrl()` (JS، مع base) لا تزال تقبل سلاسل نصية غير
   مبدوءة بـ`http(s)://` (مثل `'not-a-url'`) بتحويلها لرابط نسبي — بعكس القيد الجديد في القاعدة
   وبعكس `isValidResourceUrl()`. **لم يُصلَح عمدًا** لأنه خارج النطاق المعتمَد صراحة (الخطة سمحت
   فقط بإصلاح حالة الفراغ). التأثير العملي معدوم اليوم بفضل القيد الجديد الذي يمنع مثل هذه القيم
   من دخول القاعدة أصلًا من الآن فصاعدًا. إن رغبت بإغلاق هذا التباين أيضًا، يحتاج قرارًا وخطة
   منفصلة (توسيع نطاق `safeResourceUrl()` ليطابق تمامًا نمط القيد الجديد بدل الاعتماد على `base`).
2. **القاعدة الحية لا تزال فارغة من الموارد** — كل الاختبارات أعلاه صحيحة ومثبَتة فعليًا، لكن لم
   يُختبَر القيد بعد ضد بيانات إنتاج حقيقية (لأنها غير موجودة بعد) — سيُختبَر ضمنيًا أول مرة تُضاف
   فيها موارد حقيقية عبر `admin.js`.
3. **لا أداة متصفح فعلي متاحة لي** — اختبارات JS (T11/T12 + مقارنة `not-a-url`) نُفِّذت عبر تشغيل
   نفس كود الدالتين الحقيقي مباشرة في Node (استخراج نص الدالة من الملف الفعلي وتنفيذه)، وليس داخل
   متصفح حقيقي، لكنها اختبار فعلي للكود الحقيقي المعدَّل، وليست محاكاة نظرية أو افتراضًا.
4. **`faculties`/`years` الموجودة مسبقًا في القاعدة (3 كليات، سنة واحدة)** لم تُذكَر في تقرير الـ
   Audit السابق (كان النطاق هناك مركّزًا على `resources`/`subjects`/`universities` فقط) — لاحظتها
   أثناء اختبارات هذا الطلب واستخدمتها فقط للتحقق من عدم تأثرها (كانت موجودة قبل هذه الجلسة، لم
   ألمسها بأي كتابة دائمة). لا علاقة لها بـ`file_url` وليست جزءًا من نطاق P1-6.
