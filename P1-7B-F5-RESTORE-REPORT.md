# تقرير — P1-7B: إصلاح F5 مع الحفاظ على First Session Wins

## الحالة النهائية: **PASS**

---

## 1. المشكلة التي كانت تسبب رفض F5

بعد تحويل السلوك إلى **First Session Wins** (الطلب السابق)، أصبحت `acquire_admin_session_lock()`
ترفض أي محاولة ثانية للحصول على القفل ما دام هناك قفل فعّال — **حتى لو كانت المحاولة من نفس
الحساب**، ولا يوجد أي استثناء لصاحب القفل الحالي (هذا هو المطلوب أصلًا).

المشكلة: `currentLockToken` في `admin/admin.js` كان يعيش **فقط** في متغير module-level JS، بلا
أي تخزين مستمر. عند F5:

1. الصفحة تُعاد من الصفر، `currentLockToken` يبدأ من `null`.
2. Supabase تحتفظ بجلسة المصادقة (session) عادةً، فـ `checkAuthAndInit()` يجد المستخدم لا يزال
   مسجَّل الدخول ويكمل المسار تلقائيًا حتى `enterDashboardWithLock()`.
3. `enterDashboardWithLock()` كان يستدعي `acquireAdminLock()` مباشرة — أي `acquire_admin_session_lock()`
   من جديد، كأنها محاولة دخول ثانية بالكامل.
4. بما أن القفل لا يزال فعّالًا (ملك هذا المستخدم نفسه من قبل F5)، والدالة (بعد التعديل السابق)
   لا تملك أي استثناء لصاحب القفل الحالي → `acquired:false, reason:'locked'` → طرد فوري
   (`showLogin` + `signOut`).

اكتشفت أثناء الفحص **مصدر تعقيد إضافي** لم يُذكر في وصف المشكلة: كان يوجد أيضًا معالج
`window.addEventListener("pagehide", ...)` يحاول تحرير القفل (`release_admin_session_lock`) عبر
`fetch(..., {keepalive:true})` عند **أي** تفريغ للصفحة — بما فيها F5 نفسها (`pagehide` يُطلَق
عند reload تمامًا كما يُطلَق عند إغلاق التبويب). هذا يعني أن أي حل يعتمد فقط على "تذكّر التوكن"
بعد F5 كان سيدخل في سباق شبكة (race) غير محدد النتيجة مع طلب هذا التحرير — أحيانًا يُنهي تحريرُ
pagehide القفلَ قبل أن تصل محاولة الاستعادة، فتفشل الاستعادة وتُضطر لعمل acquire جديد بتوكن
جديد رغم أنها مجرد F5. هذا كان سيجعل أي إصلاح غير موثوق (يعمل أحيانًا ويفشل أحيانًا).

---

## 2. الفحص الذي سبق اختيار الحل (كما طُلب في §8)

- `currentLockToken`: متغير module-level وحيد، بلا أي `localStorage`/`sessionStorage` — يُفقد
  فورًا عند أي إعادة تحميل.
- `enterDashboardWithLock()`: نقطة الدخول الموحّدة (بعد تسجيل الدخول المباشر أو بعد MFA) —
  تستدعي `acquireAdminLock()` دائمًا دون أي فحص مسبق لوجود قفل يملكه هذا التبويب أصلًا.
- `refresh_admin_session_lock(p_session_token uuid)` (SQL، غير معدَّلة): تتحقق من ثلاثة شروط
  معًا قبل أي نجاح — `auth.uid() = user_id` **و** `session_token = p_session_token` **و**
  `expires_at >= now()`. عند النجاح: تُمدِّد `expires_at` فقط، لا تُنشئ أي توكن جديد ولا تُغيّر
  صاحب القفل. هذه بالضبط دلالة "استعادة قفل مملوك مسبقًا" التي نحتاجها — دالة موجودة وجاهزة، لا
  حاجة لأي دالة/جدول SQL جديد.
- معالج `pagehide`: يحرر القفل عند أي `pagehide`، بما فيها F5 — مصدر السباق الموصوف أعلاه.

**الخلاصة**: أقل تغيير ممكن هو تخزين `session_token` في **`sessionStorage`** (وليس
`localStorage`) بعد كل `acquire` ناجح، ومحاولة "استعادته" عبر استدعاء `refresh_admin_session_lock()`
**نفسها** (بلا أي تعديل عليها) قبل أي `acquire` جديد، مع إزالة معالج `pagehide` الذي يتعارض مع
هذه الاستعادة. لا حاجة لأي دالة/عمود/جدول SQL جديد.

---

## 3. الحل المختار ولماذا هو آمن

### لماذا `sessionStorage` تحديدًا (وليس `localStorage` أو تخزين آخر)؟

`localStorage` **مشترك بين كل تبويبات نفس الأصل (origin)** في نفس المتصفح — لو استُخدم هنا، لكان
أي تبويب ثانٍ فعلي (حتى لو فتحه نفس المستخدم عمدًا لا بالخطأ) سيرث نفس التوكن تلقائيًا ويستطيع
"استعادة" قفل التبويب الأول، أي يُبطل ميزة "First Session Wins" بالكامل للتبويبات المتعددة —
بالضبط السيناريو الممنوع صراحة في الطلب (§6/§7 ونقطة "لا تستخدم WHERE user_id = auth.uid() وحده").

`sessionStorage` **معزولة لكل تبويب على حدة** (كل تبويب/نافذة جديدة يبدأ بـ `sessionStorage`
فارغة حتى لنفس الأصل ونفس تسجيل الدخول)، وتُمسح تلقائيًا عند إغلاق التبويب فعليًا. هذا يطابق
تمامًا ما نريده: **استمرارية عبر F5 لنفس التبويب فقط**، بلا أي مشاركة مع أي تبويب/متصفح آخر.

### لماذا لا يكفي تطابق `auth.uid()` وحده؟

`refresh_admin_session_lock()` (المستخدَمة هنا للاستعادة) تشترط **معًا**: `auth.uid()` **و**
`session_token` **و** عدم انتهاء الصلاحية. لو اعتمدنا على `auth.uid()` وحده (كما حذّر الطلب
صراحة)، لكان أي متصفح/تبويب ثانٍ فعلي لنفس الحساب (له نفس `auth.uid()` بالضرورة) قادرًا على
"استعادة" قفل التبويب الأول فقط لأنه نفس المستخدم — وهذا بالضبط ما كان يحدث في الخلل القديم قبل
تعديل "First Session Wins". الحماية الفعلية هنا هي `session_token`: قيمة `UUID` عشوائية لا يعرفها
إلا التبويب الذي فاز بالقفل فعلًا (أُعيدت له في استجابة RPC واحدة فقط)، ومخزَّنة في `sessionStorage`
الخاصة بذلك التبويب حصرًا — تبويب/متصفح آخر لنفس الحساب لا يملك هذه القيمة إطلاقًا، فمحاولة
استعادته تفشل حتمًا (`ok:false, reason:'not_owner_or_expired'` — لأن `session_token` لا يطابق
شيئًا لديه أصلًا، فهو سيحاول acquire عاديًا بدلًا من ذلك ويُرفض بـ `locked`).

### التسلسل الجديد في `enterDashboardWithLock()`

```
1. restoreAdminLock():
   - اقرأ session_token من sessionStorage (خاص بهذا التبويب).
   - إن لم يوجد → false فورًا (لا استدعاء شبكة عبثًا؛ هذا هو مسار "تسجيل دخول جديد" العادي تمامًا، بلا أي تغيير فيه).
   - إن وُجد → استدعِ refresh_admin_session_lock(token):
       - نجاح (ok:true) → currentLockToken = token (نفسه، بلا تغيير)، ابدأ heartbeat، أرجِع true. لا acquire إطلاقًا.
       - فشل → امسح sessionStorage، أرجِع false.
2. إن لم تنجح الاستعادة → acquireAdminLock() تمامًا كما كان قبل هذا التعديل (بلا أي تغيير في هذه الدالة سوى حفظ التوكن في sessionStorage بعد النجاح).
```

### إزالة معالج `pagehide`

كان يحاول تحرير القفل عند أي تفريغ للصفحة (بما فيها F5) بطريقة "غير مضمونة" أصلًا (التعليق
الأصلي في الكود يوثّق ذلك بوضوح: "لا ضمانة لنجاحه... الضامن الحقيقي يبقى TTL"). إبقاؤه كان يخلق
سباقًا غير محدد النتيجة مع `restoreAdminLock()` الجديدة، وأحيانًا يجعل F5 يحصل على قفل جديد رغم
أن المطلوب صراحة هو ألا يحدث ذلك. إزالته **ضرورية ومباشرة** لإصلاح F5 بشكل حتمي (وليست تحسينًا
جانبيًا)، ولا تُفقِد أي ضمان حقيقي: TTL (90 ثانية، بلا أي تغيير) يبقى — كما كان دائمًا موثَّقًا —
الضامن الفعلي الوحيد لتحرير قفل تبويب أُغلق أو تعطّل جهازه فعليًا.

---

## 4. الملفات التي تغيّرت

| الملف | نوع التغيير |
|---|---|
| `admin/admin.js` | **معدَّل فقط** (لا ملفات SQL جديدة، لا migration، لا بنية قاعدة بيانات جديدة إطلاقًا) |
| كل ملف/دالة/جدول آخر (بما فيه كل ملفات `sql/`، `js/auth.js`، `js/app.js`، `admin/index.html`، الصفحات العامة) | **بدون أي تغيير** — تحقّقت عبر `diff -rq` كامل، والتحقق الحي على القاعدة (§7) يؤكد أن الدوال الأربع (`acquire/refresh/release_admin_session_lock`, `fn_is_super_admin`) وRLS وGRANTs كلها مطابقة تمامًا لما كانت عليه قبل هذا الطلب |

**لا migration جديد ولا تغيير حي على القاعدة في هذا الطلب** — الحل بالكامل من طرف الواجهة، معتمِد
على دالة `refresh_admin_session_lock()` الموجودة أصلًا بلا أي تعديل عليها، تمامًا كما طُلب صراحة
("لا تطبق أي migration... لا تضف بنية قاعدة بيانات جديدة إلا إذا كانت ضرورية فعلًا" — لم تكن
ضرورية هنا).

## 5. التغييرات الدقيقة في `admin/admin.js`

1. ثابت جديد: `LOCK_TOKEN_STORAGE_KEY` (مفتاح `sessionStorage`).
2. `acquireAdminLock()`: إضافة سطر واحد يحفظ `session_token` في `sessionStorage` بعد نجاح
   `acquire` (لا تغيير على منطقها الأصلي).
3. دالة جديدة `restoreAdminLock()`: تقرأ `sessionStorage`، وتستدعي `refresh_admin_session_lock()`
   الموجودة أصلًا (بلا أي تعديل عليها) للتحقق من الملكية واستعادة القفل دون توكن جديد.
4. `forceLockLogout()` و`releaseAdminLock()`: إضافة سطر `sessionStorage.removeItem(...)` في كل
   منهما (تنظيف عند إنهاء الجلسة، لا منطق جديد).
5. `enterDashboardWithLock()`: محاولة `restoreAdminLock()` أولًا؛ عند فشلها فقط تُستدعى
   `acquireAdminLock()` تمامًا كما كانت الدالة تعمل قبل هذا التعديل — لا تغيير آخر في تدفقها.
6. إزالة معالج `pagehide` (والمتغير `lastKnownAccessToken` وتسجيل `onAuthStateChange` الداعمين
   له حصرًا — لا استخدام آخر لهما في أي مكان في الملف، تحقّقت بالبحث الكامل) — السبب موضَّح في
   §3 أعلاه، وتُرك تعليق مكانه يشرح القرار.

لا تغيير على: `startLockHeartbeat`/`stopLockHeartbeat`، `TTL`/`LOCK_HEARTBEAT_MS`، شاشات MFA،
`checkAuthAndInit`/`loadCurrentUserAuthorization`، منطق الصلاحيات (`hasPerm`/`hasAnyPerm`)، أو أي
جزء آخر من الملف.

---

## 6. نتائج الاختبارات (Tests 1–6)

نُفّذت جميعها حيًا على مشروع Supabase الفعلي داخل معاملة واحدة `BEGIN ... ROLLBACK`، لأن قفلًا
إداريًا حقيقيًا (لمستخدم `super_admin` فعلي) كان لا يزال فعّالًا وقت التنفيذ — تحققت بعد
`ROLLBACK` أن الصف عاد تمامًا كما كان (نفس `session_token` الحقيقي، heartbeat مستمر بلا انقطاع).

بما أنه لا توجد أداة متصفح فعلي متاحة لي، حاكيت بالضبط ما سيفعله `admin.js` المعدَّل: خزّنت
`session_token` بعد `acquire` (مكافئ `sessionStorage` للتبويب)، ثم استدعيت `refresh_admin_session_lock()`
بنفس التوكن لمحاكاة `restoreAdminLock()` عند F5 — وهو *حرفيًا* نفس استدعاء RPC الذي سينفّذه الكود
الجديد.

| Test | الوصف | النتيجة الفعلية | الحالة |
|---|---|---|---|
| **Test 1** | Browser A تسجّل الدخول (super_admin)، تحصل على Lock | `acquired:true` + `session_token` جديد | **PASS** |
| **Test 2** | محاكاة F5: استدعاء `refresh_admin_session_lock()` بنفس التوكن المخزَّن | `ok:true`، `session_token` في صف القفل **مطابق حرفيًا** لما كان قبل "F5" (`fa9b4791-be97-4ff2-97ce-725f7ba7d0d9` قبل وبعد) — لا توكن جديد إطلاقًا | **PASS** |
| **Test 3** | Browser B (نفس الحساب، بلا أي توكن في تخزينها) يحاول acquire أثناء نشاط A | `acquired:false, reason:'locked'`؛ صف القفل يبقى بنفس توكن A دون أي تغيير | **PASS** |
| **Test 4** | حساب إداري *مختلف* يحاول الدخول أثناء نشاط A | `acquired:false, reason:'locked'`؛ قفل A دون أي تغيير | **PASS** |
| **Test 5** | A يسجّل خروجًا (`release_admin_session_lock`) | يُرجع `true`، الصف يُصفَّر بالكامل → ثم B يحاول الدخول | `acquired:true` فورًا | **PASS** |
| **Test 6** | انتهاء TTL لجلسة B (محاكاة بتصفير `expires_at`) — محاولة B استعادة قفلها تفشل، ثم A تسجّل دخولًا جديدًا | استعادة B: `ok:false, reason:'not_owner_or_expired'` | دخول A: `acquired:true` بتوكن جديد | **PASS** |
| تحقّق تنظيف نهائي | الصف الحقيقي في الإنتاج بعد `ROLLBACK` | مطابق تمامًا لما كان قبل الاختبار (نفس `session_token` الحقيقي `a8e81d2b-...`، heartbeat مستمر) | **PASS** |

---

## 7. إثبات أن Browser B لا يستطيع أخذ Lock من A

في **Test 3**، لم تُستدعَ `restoreAdminLock()`/`refresh_admin_session_lock()` نيابةً عن B إطلاقًا —
لأن B (في محاكاتي، تمامًا كما سيحدث في متصفح حقيقي) **لا تملك أي `session_token` مخزَّن في
`sessionStorage` الخاصة بها** (هذا التخزين معزول لكل تبويب). لذلك مسارها الوحيد هو `acquireAdminLock()`
العادية، التي تُرفض حتمًا (`locked`) ما دام قفل A فعّالًا — بنفس آلية "First Session Wins" التي
طُبِّقت في الطلب السابق، دون أي تغيير عليها في هذا الطلب. تحقّقت أيضًا في **Test 4** أن نفس
المنطق يصمد لحساب مختلف تمامًا. في الحالتين: `session_token` في صف القفل نفسه لم يتغيّر ولا مرة
واحدة أثناء محاولات B — دليل مباشر أن قفل A لم يُلمَس.

## 8. إثبات أن F5 لا يحوّل الجلسة الأولى إلى Session جديدة

في **Test 2**، القيمة المرجعة من `T1_A_acquire` للتوكن (`fa9b4791-be97-4ff2-97ce-725f7ba7d0d9`)
هي **نفسها بالضبط** الموجودة في صف `admin_session_lock` بعد استدعاء `refresh_admin_session_lock()`
(محاكاة F5) — لا `acquire_admin_session_lock()` استُدعيت في هذا المسار إطلاقًا (لم يظهر أي سطر
`T2_...acquire...` في السجل، فقط `refresh`)، ولا أي توكن جديد صدر. هذا يطابق تمامًا التسلسل الذي
سينفّذه `restoreAdminLock()` الجديد في `admin.js`: عند نجاح الاستعادة، الكود لا يستدعي
`acquireAdminLock()` مطلقًا (`const acquired = restored || (await acquireAdminLock())` — التقييم
القصير يمنع الاستدعاء الثاني تمامًا إن نجحت الاستعادة).

---

## 9. فحص عدم وجود Regression

- `acquire_admin_session_lock()` / `refresh_admin_session_lock()` / `release_admin_session_lock()`
  (SQL): **بلا أي تعديل** في هذا الطلب — تحقّق حي (`pg_get_functiondef`) يطابق تمامًا نسخة الطلب
  السابق (Phase 4C).
- `fn_is_super_admin()`: بلا أي `CREATE OR REPLACE` جديد — تحقّق حي.
- `fn_has_permission()`: لا تزال بنسختيها (2 overloads) كما كانت — تحقّق حي.
- MFA flow: `js/auth.js` بلا أي تغيير؛ `enterDashboardWithLock()` لا تزال تُستدعى بعد MFA فقط
  (السطر `await enterDashboardWithLock(currentAuthEmail);` في معالج التحقق بخطوتين لم يتغيّر).
- RLS: `admin_session_lock` لا تزال `RLS ENABLED = true`, `policy_count = 0` — تحقّق حي.
- GRANT/REVOKE: لم تتغيّر (لا حاجة، لا دالة SQL جديدة ولا معدَّلة في هذا الطلب).
- TTL: **لم يتغيّر** — لا يزال 90 ثانية ثابتة من جهة القاعدة، و`LOCK_HEARTBEAT_MS` لا يزال 25000
  في `admin.js`.
- `node --check admin/admin.js`: **نجح** (لا أخطاء صياغة).
- `diff -rq` كامل بين النسخة الأصلية المرفوعة والنسخة النهائية: الفرق الوحيد هو `admin/admin.js`
  (بالإضافة إلى ملفَي الطلب السابق `sql/phase4c_p1_7b_first_session_wins.sql` والتقرير المرافق
  له، غير المتأثرين بهذا الطلب). لا شيء آخر تغيّر.
- التغيير محصور فعليًا في إصلاح F5: كل تعديل في `admin.js` إما (أ) تخزين/قراءة/مسح توكن في
  `sessionStorage`، (ب) دالة استعادة جديدة تستدعي RPC موجودة أصلًا، أو (ج) إزالة معالج `pagehide`
  الذي كان يتعارض مباشرة مع هذا الإصلاح — لا شيء آخر.

---

## 10. قيود متبقية

1. **لا أداة متصفح فعلي متاحة لي** — لم أُشاهد سلوك F5/`sessionStorage` بصريًا داخل متصفح حقيقي؛
   الاختبار تم عبر محاكاة استدعاءات RPC المطابقة حرفيًا لما سينفّذه الكود الجديد (نفس القيد
   المذكور في تقارير P1-7B السابقة لهذا المشروع).
2. **حالة "Duplicate Tab" في بعض المتصفحات**: بعض المتصفحات (مثل Chrome عبر قائمة "Duplicate")
   تنسخ `sessionStorage` إلى التبويب الجديد المُنسوخ. هذا يعني أن تبويبًا مُستنسَخًا فعليًا من
   تبويب A (وليس "متصفح ثانٍ" بمعنى تسجيل دخول جديد) سيرث نفس التوكن ويمكنه استعادة نفس القفل —
   وهذا **متوقع ومقصود** ضمن حدود تصميم `sessionStorage` (يُعامَل كامتداد لنفس الجلسة، لا كجلسة
   ثانية منفصلة)، وليس ثغرة تسمح بتجاوز "First Session Wins" لأي تسجيل دخول جديد فعلي — لم يُطلب
   التعامل مع هذه الحالة الحدّية في التعليمات، أذكرها هنا فقط للعلم.
3. **إغلاق تبويب/متصفح فعلي** الآن يعتمد **حصرًا** على TTL (90 ثانية) لتحرير القفل، بعد إزالة
   محاولة `pagehide` غير المضمونة أصلًا — هذا تراجع بسيط (تأخير أقصاه 90 ثانية بدل محاولة فورية
   غير مضمونة) كان **ضروريًا** لإصلاح F5 بشكل حتمي، وموضَّح بالكامل في §3.
4. لا تغيير على القيد المعروف مسبقًا بخصوص عدم اختبار تزامن حقيقي (true concurrency) بين اتصالين
   منفصلين فعليًا — نفس القيد الموثَّق في تقارير P1-7B السابقة، غير متأثر بهذا الطلب.
