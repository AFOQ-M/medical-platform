# P1-6 Audit Report

**نوع هذه المرحلة: Audit فقط — لم يُعدَّل أي ملف أو SQL أو بيانات. كل استعلامات القاعدة كانت
`SELECT`/metadata فقط، على القاعدة الحية فعلًا (بلا `BEGIN/ROLLBACK` — لم تكن ضرورية لأنها
قراءة بحتة).**

---

## 1. Executive Summary

`resources.file_url` محميّ اليوم عبر **طبقتين من التحقق من طرف العميل (client-side) فقط**،
لا أي طبقة من طرف القاعدة:

1. **وقت الكتابة** — نموذج المورد في `admin/admin.js` (`isValidResourceUrl`) يرفض أي قيمة ليست
   `http://`/`https://` مطلقة، على مسار الإضافة والتعديل معًا (نفس المعالِج). مدعوم أيضًا بـ
   `<input type="url" required>` في `admin/index.html` كتحقق متصفح إضافي.
2. **وقت العرض** — كل صفحة عامة تعرض الموارد (`index.html`, `subject.html`, `search.html`,
   `favorites.html`) تمرّ **حصرًا** عبر دالة واحدة `buildResourceCard()` في `js/app.js`، التي
   تستخدم `safeResourceUrl()` لقصر بروتوكول الرابط على `http`/`https` فقط قبل وضعه في `href`؛ أي
   قيمة أخرى (`javascript:`, `data:`, نص غير قابل للتحليل) تتحول إلى `"#"`. تحقّقت أن هذا هو
   **المسار الوحيد** الذي يضع `file_url` في `href` في كامل المستودع (بحث شامل، لا استثناء واحد).

**الفجوة الحقيقية**: لا يوجد أي `CHECK constraint`، `trigger`، أو تحقق داخل RLS/RPC على مستوى
القاعدة يفرض هذا الشكل على `file_url` — بعكس كل عمود آخر في نفس الجدول (`type`, `language`,
`status`, `source_type`, `storage_provider`) الذي له `CHECK constraint` صريح. الحماية بأكملها
اليوم تعتمد على انضباط كود JavaScript، بلا "شبكة أمان" من القاعدة.

**لا يوجد استغلال فعلي ممكن اليوم عبر الواجهة العادية** (الطبقتان تعملان بشكل صحيح ومتّسق)، لكن أي
مسار كتابة مستقبلي يتجاوز `admin.js` (نداء مباشر لـ REST API من حساب `staff`/`admin` له صلاحية
إضافة/تعديل موارد) يمكنه اليوم إدخال `file_url` بأي قيمة — بما فيها `javascript:`/`data:`/نص فارغ
— بلا أي رفض من القاعدة. هذا لا يُستغَل حاليًا بفضل `safeResourceUrl()` عند العرض، لكنه يجعل أمان
النظام بأكمله معتمدًا على نقطة فحص واحدة (Single Point of Failure) بلا نسخة احتياطية من القاعدة.

**القاعدة الحية فارغة فعليًا** (0 موارد، 0 مواد، جامعة واحدة فقط) — لا توجد بيانات حقيقية لفحصها
لوجود روابط فعلية فاسدة؛ كل النتائج أدناه تحليل كود + بنية قاعدة، وليس فحص بيانات إنتاج حقيقية.

---

## 2. Original P1-6 Requirement

**لا يوجد مستند/قسم مخصّص باسم "P1-6" في المستودع.** لا `README.md` ولا أي ملف `.md` آخر يذكر
"P1-6" إطلاقًا (تحقّقت ببحث شامل). المصدر الوحيد المتاح لتحديد ما هو P1-6 هو تعليق داخل الكود
نفسه في `admin/admin.js` (السطر 1257 وما حوله)، والذي يصف الدالة `isValidResourceUrl()` بأنها
تطبيق P1-6:

> "P1-6: يتحقق أن رابط الملف عنوان URL مطلق بمخطّط http/https فقط"

بناءً على هذا التعليق + نمط بقية بنود P0/P1 المشابهة في المستودع (كلها بنود Security/Hardening
مضافة تدريجيًا: P0-3/P0-4 rate limiting، P1-1 pagination، P1-2 fetch cap، P1-4 is_active gating،
P1-5 توثيق سريع، P1-7/P1-7B MFA + Session Lock) — الاستنتاج الأرجح هو أن **P1-6 بند صحّة/أمان
مدخلات لـ`file_url`**: منع إدخال قيم خطرة أو غير صالحة (خصوصًا مخطّطات URL قابلة للتنفيذ مثل
`javascript:`) في حقل نصي حر يُستخدم لاحقًا كرابط قابل للنقر (`href`) على الموقع العام.

**لم أفترض** أن المطلوب هو "URL format validation" فقط دون تحقق — الفحص الكامل أدناه (§5–§8) يغطي
مسار الكتابة والعرض معًا، وليس فقط صيغة الرابط.

⚠️ **ملاحظة مهمة يجب إخبارك بها صراحة**: بما أنه لا يوجد نص مطلوب أصلي رسمي (Acceptance
Criteria) مكتوب في أي مكان غير هذا التعليق البرمجي، فإن أي تصنيف "مكتمل/غير مكتمل" أدناه هو
**استنتاج مبني على أفضل تفسير متاح للنية**، وليس مطابقة لمواصفة موثّقة. أنصح أن تؤكد أو تصحّح فهمي
لما تريده P1-6 أن تغطيه قبل أي قرار تنفيذ.

---

## 3. Current Implementation

| الطبقة | الملف | الآلية |
|---|---|---|
| **وقت الكتابة (Admin)** | `admin/admin.js:1267` `isValidResourceUrl()` | `new URL(rawUrl)` (بدون base) → يقبل فقط `protocol === 'http:' \|\| 'https:'` |
| نفس الطبقة | `admin/admin.js:1281-1285` | داخل معالِج `submit` لنموذج المورد — يُشغَّل **قبل** أي `insert`/`update`؛ نفس المعالِج يغطي الإضافة *و* التعديل (متغيّر `id` يحدد أيهما) |
| نفس الطبقة (متصفح) | `admin/index.html:286` | `<input type="url" id="res-file-url" required>` — تحقق متصفح إضافي (HTML5) قبل حتى وصول القيمة لـ JS |
| **وقت العرض (عام)** | `js/app.js:127` `safeResourceUrl()` | `new URL(url, window.location.href)` (**مع** base) → يُرجع الرابط فقط إن كان `http:`/`https:`، وإلا `"#"` |
| نفس الطبقة | `js/app.js:198` `buildResourceCard()` | نقطة الاستخدام الوحيدة: `openBtn.href = safeResourceUrl(resource.file_url)` |
| **القاعدة** | `sql/schema.sql:47` | `file_url text not null` — **بلا أي `CHECK`** (بعكس كل عمود آخر بجانبه) |
| **RLS** | policies على `resources` (INSERT/UPDATE) | تتحقق من `fn_has_permission()` فقط (من له صلاحية الكتابة) — **لا تفحص محتوى `file_url` إطلاقًا** |
| **RPC** | `search_resources()`, `increment_resource_view()` | `file_url` يُمرَّر كما هو دون أي معالجة في كليهما؛ `increment_resource_view` لا يلمسه إطلاقًا |
| **Triggers** | — | **لا يوجد أي trigger** على جدول `resources` (تحقّق حي: نتيجة فارغة) |

---

## 4. Database Findings

تحقّقت هذه كلها حيًا (قراءة فقط) على مشروع Supabase الفعلي (`lzmkgfxlsynaphpofblb`):

- **النوع**: `file_url` هو `text`.
- **Nullable؟** لا — `is_nullable = 'NO'` (قيد `NOT NULL` فقط، لا قيمة افتراضية).
- **Constraints على `resources`**: 7 قيود إجمالًا — `pkey`, `fkey (subject_id)`, و`CHECK` على
  كل من `type`, `language`, `source_type`, `status`, `storage_provider`. **لا يوجد أي `CHECK` على
  `file_url`**.
- **Triggers**: لا يوجد أي trigger مرتبط بـ`resources` (استعلام `pg_trigger` أعاد نتيجة فارغة).
- **Functions تتحقق من `file_url`**: لا يوجد — لا في `search_resources()` ولا
  `increment_resource_view()` ولا أي دالة أخرى بحثت عنها.
- **RLS**: مفعّلة على `resources`. سياسات `INSERT`/`UPDATE`/`DELETE` تعتمد بالكامل على
  `fn_has_permission('resources', university_id, faculty_id, action)` — تتحقق من **صلاحية
  المستخدم فقط**، صفر تحقق من **محتوى** `file_url`. سياسة `UPDATE` تحديدًا: `with_check` فارغ
  (`null`)، ما يعني أن Postgres يطبّق نفس تعبير `USING` تلقائيًا كـ `WITH CHECK` الفعلي — وهو
  نفسه تعبير الصلاحية فقط، فلا تغيير في الاستنتاج.
- **Supabase Storage**: **غير مستخدَم إطلاقًا في هذا المشروع** — تحقّقت أن `storage.buckets` فارغ
  تمامًا (0 صف)، ولا يوجد أي استدعاء لـ`supabaseClient.storage.*` في أي ملف JS بالمستودع. قيم
  `storage_provider` (`google_drive`, `backblaze`, `telegram`, `external`) هي **بيانات وصفية نصية
  فقط** تصف أين يستضاف الملف فعليًا خارجيًا — لا علاقة لها بميزة Supabase Storage نفسها. هذا يعني
  أن **كل** قيم `file_url` المتوقَّعة هي روابط خارجية (external URLs) بطبيعتها، بصرف النظر عن قيمة
  `storage_provider`.

---

## 5. Frontend Findings

بحثت عن كل استخدام لـ`.file_url` في كامل المستودع (`grep` شامل، 3 نتائج فقط):

1. `js/app.js:75` (`pickResourceFields`) — ينسخ `file_url` الخام إلى كائن يُخزَّن في
   `localStorage` (مفضّلة/آخر ما شوهد). **لا** يُستخدم كـ `href` هنا مباشرة.
2. `js/app.js:198` (`buildResourceCard`) — **النقطة الوحيدة في كامل المستودع** التي تضع
   `file_url` في `href`، عبر `safeResourceUrl()`.
3. `admin/admin.js:1325` (`editResource`) — يضع القيمة الخام في `.value` لعنصر `<input>` عند فتح
   نموذج التعديل. هذا **آمن بطبيعته**: `.value` على عنصر إدخال هو خاصية نصية، لا يُفسَّرها
   المتصفح كـ HTML أو ينفّذها — لا مسار XSS هنا حتى لو كانت القيمة `javascript:...`.

**تأكدت أن `buildResourceCard()` هي نقطة الاستخدام الوحيدة والحصرية** لبناء رابط قابل للنقر من
`file_url` عبر كل صفحات الموقع العام: `index.html` (قسمَي "تابع من حيث توقفت" و"الأكثر
طلبًا/الأحدث")، `subject.html`، `search.html`، و`favorites.html` — **بما فيها** الموارد المُعاد
عرضها من `localStorage` (مفضّلة/آخر ما شوهد)، لأن `buildResourceCard()` تُستدعى عليها أيضًا في كل
مرة (وليس القيمة المخزَّنة سابقًا تُستخدَم كـ`href` مباشرة) — أي أن `safeResourceUrl()` يُعاد
تطبيقه في كل مرة يُعرَض فيها المورد، بصرف النظر عن مصدر البيانات (قاعدة مباشرة أو ذاكرة محلية).

جدول `resources` في **لوحة التحكم** (`renderResourcesTable`) **لا يعرض `file_url` كرابط
إطلاقًا** — العمود غير موجود في الجدول أصلًا (فقط العنوان/الموقع/النوع/الحالة/المشاهدات/أزرار
إجراءات)، فلا مسار XSS هناك بغض النظر عن قيمة `file_url`.

**لم أجد أي استخدام آخر لـ`file_url`** عبر `window.open`, `location`, `location.href`,
`innerHTML`, أو `insertAdjacentHTML` في أي مكان بالمستودع (بحثت عن كل هذه الأنماط صراحة).

---

## 6. Security Findings

| السيناريو | ما يحدث فعليًا اليوم | الطبقة المسؤولة |
|---|---|---|
| `https://example.com/file.pdf` | يُقبَل، يُعرَض كرابط فعّال | كلتا الطبقتين تقبله |
| `http://example.com/file.pdf` | يُقبَل (المشروع **لا** يفرض HTTPS حصرًا لا كتابةً ولا عرضًا) | كلتا الطبقتين تقبله بالتساوي |
| `""` (فارغ) | **مرفوض عند الكتابة عبر واجهة الأدمن** (`!fileUrl` في `admin.js:1282` + `required` في HTML) — لكن **غير مرفوض على مستوى القاعدة** إن أُدخل عبر مسار آخر (`NOT NULL` يسمح بسلسلة فارغة، فهي ليست `NULL`) | فجوة: طبقة الكتابة تعتمد على الواجهة فقط |
| `"   "` (whitespace) | نفس حالة الفارغ تقريبًا: `admin.js` يستخدم `.trim()` قبل الفحص فيرفضها في واجهة الأدمن، لكن القاعدة لا تمنعها إطلاقًا |
| `javascript:...` | **مرفوض** في كلتا الطبقتين (`isValidResourceUrl` عند الكتابة عبر الواجهة، و`safeResourceUrl` يحوّله لـ `"#"` عند العرض دائمًا مهما كان مصدره) | محمي بالكامل في المسارات الموجودة |
| `data:...` | نفس حالة `javascript:` — مرفوض في الطبقتين | محمي بالكامل |
| `not-a-url` (malformed) | `new URL()` يرمي استثناء في كلتا الدالتين → مرفوض عند الكتابة عبر الواجهة، يتحول لـ`"#"` عند العرض | محمي في المسارات الموجودة |
| `https://unknown-domain.example/...` | **يُقبَل بلا أي قيود على النطاق (domain allowlist/denylist)** — لا يوجد أي تحقق من نطاق الرابط في أي مكان، فقط من المخطّط (`http`/`https`) | لا فجوة أمنية مباشرة هنا (هذا سلوك متعمَّد: المشروع يسمح بروابط خارجية متعددة المزوّدين حسب `storage_provider`)، لكن يعني أنه لا يوجد أي حماية Open-Redirect-Adjacent إن اعتُبر ذلك مطلوبًا لاحقًا |
| Supabase Storage URL | **لا ينطبق فعليًا** — لا يوجد استخدام لميزة Supabase Storage في هذا المشروع إطلاقًا (§4) |

**تحليل مسار الاستغلال النظري (لم يُختبَر فعليًا في متصفح — تحليل كود فقط)**:
حساب له صلاحية `resources: edit/create` (staff/admin) يستدعي PostgREST مباشرة (متجاوزًا
`admin.js`) بـ `file_url = ""` أو بقيمة تحتوي whitespace فقط: القاعدة تقبلها (لا `CHECK` يمنعها).
عند العرض، `safeResourceUrl("")` → `new URL("", location.href)` — وفق خوارزمية تحليل URL
القياسية، مرجع فارغ نسبةً لـ base يُحلّ إلى **نفس رابط الصفحة الحالية** (وليس استثناء) — أي أن
الدالة **لن** تُرجِع `"#"` كما هو المتوقَّع لحالة "رابط غير صالح"، بل رابطًا صحيحًا لكن **مضلِّلًا
وظيفيًا** (زر "فتح/تحميل" يعيد تحميل نفس الصفحة الحالية بدل فتح ملف). **هذا ليس ثغرة أمنية** (لا
تنفيذ كود، لا تسريب بيانات) بل **خلل وظيفي/data-integrity محتمل** لم يُعالَج صراحة في أي من
الطبقتين الحاليتين، ولا يظهر إلا عبر مسار كتابة يتجاوز واجهة الأدمن.

**لا يوجد أي ثغرة XSS فعلية مُثبَتة في الكود الحالي** — `buildResourceCard()` تبني العناصر عبر
`document.createElement`/`.href` (ليس `innerHTML`)، والمسار الوحيد المستخدَم دائمًا يمر عبر
`safeResourceUrl()`. `escHtml()` (المستخدَمة في مكان آخر لعرض `title` عبر `innerHTML`) لا علاقة
لها بـ`file_url` إطلاقًا — لم تُستخدَم على هذا الحقل في أي مكان.

---

## 7. Existing Data Findings

⚠️ **المشروع الحي فارغ عمليًا** — لا يمكن تقديم إحصائية ذات معنى عن بيانات حقيقية:

| المقياس | القيمة |
|---|---|
| إجمالي الموارد (`resources`) | **0** |
| المواد (`subjects`) | **0** |
| الجامعات (`universities`) | 1 |
| الملفات الشخصية (`profiles`) | 4 |

بما أن `resources` فارغ تمامًا، كل استعلامات الإحصاء المطلوبة (`null`/`empty`/`malformed`/
`https`/`http`/مخطّطات غير متوقَّعة إلخ) أعادت **0** لكل الحالات — لا لأن البيانات "نظيفة" بل لأنه
**لا توجد بيانات إطلاقًا** لتقييمها. لا يمكن الاستنتاج من هذا أن أي تصميم فعلي "يعمل بنجاح على
بيانات حقيقية" — هذا مشروع تجريبي/حديث الإنشاء بلا موارد مُدخَلة بعد.

---

## 8. Attack/Edge-Case Analysis

راجع الجدول الكامل في §6. ملخص الحالات التي **لم** تُغطَّ بشكل كامل عبر الطبقتين معًا:

1. **فارغ/whitespace يدخل عبر مسار غير واجهة الأدمن** — القاعدة لا تمنعه، والعرض
   (`safeResourceUrl`) لا يكتشفه كحالة خاصة (يُنتج رابطًا "صالحًا" لكنه ذاتي/مضلِّل بدل `"#"`
   المتوقع لأي قيمة غير صالحة). **هذا التحليل مبني على قراءة الكود وخوارزمية `URL` القياسية، لم
   أختبره فعليًا في متصفح حي (لا أداة متصفح متاحة لي).**
2. **غياب أي enforcement على مستوى القاعدة بشكل عام** — ليس ثغرة نشطة اليوم (المسار الوحيد
   الموجود فعليًا للكتابة هو نموذج الأدمن، وهو يتحقق بشكل صحيح)، لكنه فجوة defense-in-depth
   حقيقية: أي حساب لديه صلاحية كتابة موارد يستطيع نظريًا تجاوز الواجهة بالكامل عبر استدعاء
   PostgREST مباشرة، ولا شيء في القاعدة نفسها يردعه.
3. **لا قيود على نطاق الرابط (domain)** — مصمَّم عمدًا هكذا على الأرجح (روابط خارجية متعددة
   المزوّدين)، لكن أذكره صراحةً بصفته سلوكًا فعليًا لا تحققًا ناقصًا (ما لم يكن المطلوب من P1-6
   يشمل قائمة نطاقات مسموحة، وهو أمر غير مؤكَّد بلا مصدر مطلوب رسمي — راجع §2).

---

## 9. Compatibility With Previous P0/P1

لم أعدّل أي شيء في هذه المرحلة، لذا لا يوجد "كسر" فعلي بعد. للتقييم المستقبلي فقط (عند اتخاذ قرار
تنفيذ):

- **RLS على `resources`**: أي `CHECK constraint` مستقبلي على `file_url` **مستقل تمامًا** عن RLS —
  Postgres يفرض `CHECK` على مستوى الصف بغض النظر عن RLS، ولا تتداخل معه سياسات
  `fn_has_permission()` الحالية (تحقّقت أنها لا تفحص `file_url` إطلاقًا اليوم، فلا تعارض في
  الاتجاهين).
- **RPCs (`search_resources`, `increment_resource_view`)**: كلاهما لا يكتب `file_url` (قراءة/عدّاد
  مشاهدات فقط) — أي `CHECK` مستقبلي لن يؤثر عليهما.
- **صلاحيات الإدارة (`fn_has_permission`)**: غير متأثرة — منطقها مستقل تمامًا عن محتوى `file_url`.
- **نشر الموارد / عداد المشاهدات**: كلاهما يعتمدان على `status`/`view_count`، لا علاقة بـ
  `file_url`.
- **P1-7 (MFA) / P1-7B (First Session Wins + F5 restore)**: لا علاقة إطلاقًا — أنظمة منفصلة تمامًا
  (مصادقة الأدمن مقابل بيانات الموارد)، لم أفحص أو ألمس أي شيء متعلق بهما في هذه الجلسة.
- **الخلاصة**: أي حل مستقبلي محصور في `file_url`/`resources` (كما تنص P1-6) لن يمسّ أيًا من هذه
  الأنظمة طالما بقي التغيير مقصورًا على قيد/تحقق على عمود `file_url` نفسه.

---

## 10. Required Changes

**بناءً على التصنيف في §14 (الحالة B)**، إليك ما يلزم تغييره — **للمناقشة والقرار، لم يُنفَّذ أي
منه**:

1. **الأهم (فجوة معمارية حقيقية)**: إضافة `CHECK constraint` على `resources.file_url` على مستوى
   القاعدة يفرض:
   - غير فارغ بعد `trim` (يمنع `""` و`"   "`).
   - مخطّط `http`/`https` فقط (يطابق تمامًا ما تتحقق منه الواجهة اليوم، فلا يغيّر أي سلوك مقبول
     حاليًا — فقط يمنع ما هو مرفوض أصلًا من عبور مسارات مستقبلية غير الواجهة).
   - Postgres `CHECK` لا يدعم تحليل URL كامل مباشرة؛ الخيار العملي الشائع هو `regex` بسيط يتحقق من
     البادئة (`^https?://`) بعد `btrim()`، وليس تحليل URL كامل داخل القاعدة (ذلك يبقى مسؤولية
     الواجهة كما هو الآن).
2. **اختياري/أقل أولوية**: توضيح سلوك `safeResourceUrl()` لحالة السلسلة الفارغة/whitespace
   تحديدًا (إرجاع `"#"` صراحةً بدل الاعتماد على حل `URL` النسبي) — يحسّن الوضوح الوظيفي فقط، لا
   علاقة له بالأمان (`javascript:`/`data:` محميان بالفعل بشكل صحيح).
3. **يتطلب توضيحًا منك أولًا**: هل P1-6 يُقصَد بها فقط ما ورد أعلاه (صحة المخطّط/عدم الفراغ)، أم
   تشمل أيضًا قائمة نطاقات مسموحة (`storage_provider`-aware domain allowlist)؟ لا مصدر مطلوب رسمي
   متاح لي لحسم هذا (§2).

كل ما سبق **اقتراحات للمناقشة فقط** — لم يُطبَّق أي منها، ولا `migration` تم إنشاؤه في هذه
الجلسة.

---

## 11. Files That Would Need Modification

عند اتخاذ قرار التنفيذ لاحقًا (وليس الآن):

| الملف | التغيير المحتمل |
|---|---|
| ملف `sql/` migration **جديد** (لم يُنشأ بعد) | `ALTER TABLE resources ADD CONSTRAINT ... CHECK (...)` فقط — إضافي بالكامل، بلا `DROP`/`ALTER COLUMN` |
| `js/app.js` (اختياري، بند §10.2 فقط) | تعديل سطر واحد في `safeResourceUrl()` لمعاملة السلسلة الفارغة/whitespace كحالة خاصة صريحة |

**لا حاجة** لأي تعديل على `admin/admin.js` أو `admin/index.html` أو أي RLS/RPC موجودة — التحقق من
طرف الواجهة يعمل بشكل صحيح بالفعل ولا يحتاج تغييرًا وفق هذا التحليل.

---

## 12. Database Changes That Would Be Required

**لم يُطبَّق أي شيء في هذه الجلسة.** إن قررنا المضي قدمًا لاحقًا: `CHECK constraint` إضافي واحد
فقط على `resources.file_url` (راجع §10.1). لا حاجة لعمود جديد، لا جدول جديد، لا `trigger`، ولا أي
تعديل على RLS/RPC/صلاحيات — القاعدة الحية فارغة من الموارد فعليًا (§7)، فتطبيق `CHECK` كهذا لاحقًا
سيكون آمنًا فوريًا (لا بيانات موجودة تنتهك القيد).

---

## 13. Risk Assessment

| الخطر | المستوى | التبرير |
|---|---|---|
| استغلال XSS/`javascript:` فعلي اليوم عبر الواجهة العادية | **منخفض جدًا** | كل مسار عرض موجود فعليًا محمي بـ`safeResourceUrl()`، مؤكَّد بحثًا شاملاً |
| استغلال عبر تجاوز الواجهة (API مباشر) من حساب موثوق مُخترَق | **منخفض إلى متوسط** | يتطلب أصلًا صلاحية كتابة موارد (ليس مجهولًا)، لكن لا يوجد أي رادع من القاعدة إن حدث |
| خلل وظيفي (رابط "فارغ" يتصرف كرابط ذاتي مضلِّل) | **منخفض** (تجربة مستخدم، ليس أمانًا) | يتطلب أصلًا بيانات غير صالحة تجاوزت الواجهة أصلًا لتدخل القاعدة |
| كسر أي نظام آخر (RLS/RPC/P1-7/P1-7B) عند إصلاح مستقبلي محصور بـ`file_url` | **منخفض جدًا** | لا تداخل معماري مكتشف (§9) |

---

## 14. Final Audit Status

**التصنيف**: **الحالة B — P1-6 موجودة جزئيًا لكنها غير مكتملة.**

كلتا الطبقتين المطبَّقتين (تحقق وقت الكتابة في `admin.js`، وتحقق وقت العرض في `js/app.js`) تعملان
**بشكل صحيح ومتّسق تمامًا** لما تغطيانه، ومطبَّقتان في **كل** مسار كتابة/عرض موجود فعليًا في
المستودع اليوم (بحثت بشكل شامل، لا استثناء واحد). الفجوة هي **غياب طبقة ثالثة على مستوى القاعدة**
(بعكس كل عمود شقيق آخر في نفس الجدول)، ما يجعل الحماية بأكملها تعتمد حصريًا على انضباط كود
الواجهة الأمامية بلا أي شبكة أمان من الخادم — وهي الفجوة المرجَّح أن تكون هي بالضبط سبب وجود بند
"P1-6" في قائمة التدقيق أصلًا.

**PASS WITH LIMITATIONS**

(الوظيفة الأساسية المطلوبة — منع روابط خطرة من الوصول لـ`href` على الموقع العام — محقَّقة فعليًا
وبشكل صحيح اليوم؛ القيود هي: (1) لا نص مطلوب رسمي متاح للتأكد من نطاق P1-6 الكامل المقصود [§2]،
(2) غياب طبقة تحقق على مستوى القاعدة [§6, §10]، (3) حالة حدّية للفراغ/whitespace غير معالَجة صراحة
[§8]، (4) القاعدة الحية فارغة فلا يمكن تأكيد أي مما سبق ضد بيانات حقيقية [§7].)

**بانتظار قرارك بشأن نطاق التنفيذ المطلوب (إن وُجد) بناءً على هذه الأدلة — لم يُعدَّل أي شيء.**
