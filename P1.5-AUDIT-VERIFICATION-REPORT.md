# P1.5 — تقرير التحقق من الادعاءات مقابل الكود الحي

**الوضع:** تدقيق فقط (Design Auditor + Accessibility Engineer). **بلا أي تعديل** على `css/style.css` أو `DESIGN_SYSTEM.md` أو أي ملف كود.
**المرجع:** `AFOQ-DESIGN-VERIFICATION-PROMPT.md`.
**الوثائق محل التدقيق:** `DESIGN_SPEC_V1.md` / `P1.5-DESIGN-SPEC-REVISED.md`.

---

## الجزء 1 — التحقق من الادعاءات مقابل الكود الفعلي

### 1.1 Session Lock — الادعاء في الوثيقة خاطئ: **blocking وليس informative**

**ما قالته الوثيقة** (`P1.5-DESIGN-SPEC-REVISED.md`، Z.14 سطر 939 — ومنقولة من V1 سطر 765):
> "Session-lock awareness shown if lock is held elsewhere — **informative, not blocking (RLS is enforcer)**."

**ما يفعل الكود فعليًا** (`admin/admin.js`) — الاقتباسات بالدليل:

1. الداشبورد **لا تُعرض أبدًا** دون الحصول على القفل — `enterDashboardWithLock()`:
   ```js
   // admin/admin.js:250-263
   const restored = await restoreAdminLock();
   const acquired = restored || (await acquireAdminLock());
   if (!acquired) {
     currentProfile = null;
     currentPermissions = [];
     // ...
     await supabaseClient.auth.signOut();
     showLogin("يوجد مسؤول آخر يستخدم لوحة التحكم حاليًا. حاول لاحقًا.");
     return;   // لا توجد أي فرصة لعرض الداشبورد ← blocking
   }
   showDashboard(email);
   ```
2. فقدان القفل لاحقًا (heartbeat نبض 25s فاشل) → **تسجيل خروج قسري كامل** — `forceLockLogout()`:
   ```js
   // admin/admin.js:137-141 (heartbeat) و 209-225 (forceLockLogout)
   const { data, error } = await supabaseClient.rpc("refresh_admin_session_lock", {...});
   if (error || !data || data.ok !== true) {
     await forceLockLogout("تم إنهاء جلستك الحالية (جلسة أدمن أخرى بدأت، أو انتهت صلاحية جلستك). سجّل الدخول مجددًا.");
   }
   // forceLockLogout: stopLockHeartbeat(); ... await supabaseClient.auth.signOut(); showLogin(message);
   ```
3. **آلية الفرض ليست "RLS is enforcer" بل RPCs مخصصة** — النص الحرفي في تعليقات الكود (`admin/admin.js:103-107`):
   > "الفرض الحقيقي من جهة القاعدة عبر acquire/refresh/release_admin_session_lock() (SECURITY DEFINER RPCs، الجدول نفسه deny-all عبر RLS بلا أي policy). هذا الكود هنا مجرد 'مرآة' للواجهة — تمامًا كمنطق hasPerm() أعلاه — وليس مصدر الحماية الفعلي."

**التصحيح المُقترح لوصف الوثيقة (Z.14):**
> القفل **blocking وليس informative**: إذا لم يستطع التبويب الحصول/استعادة القفل، يُسجَّل خروحًا قسريًا فورًا ويعود لشاشة الدخول برسالة ("يوجد مسؤول آخر يستخدم لوحة التحكم حاليًا. حاول لاحقًا."). إذا فُقد القفل أثناء الجلسة عبر heartbeat، يُنهى تسجيل الدخول برسالة ("تم إنهاء جلستك الحالية... سجّل الدخول مجددًا."). الفرض الفعلي **من جهة القاعدة حصريًا** عبر دوال SECURITY DEFINER: `acquire_admin_session_lock` / `refresh_admin_session_lock` / `release_admin_session_lock` + جدول `admin_session_lock` بـ deny-all RLS (بلا policy) + **TTL 90 ثانية**. كود الواجهة ليس حارسًا بل مرآة فقط. صلاحية العنوان سطر 939 "informative" يجب حذفها.

معلومة إضافية دقيقة: `LOCK_HEARTBEAT_MS = 25000` مع TTL=90s تعني ~3 محاولات heartbeat قبل الانتهاء؛ استعادة F5 تتم عبر `restoreAdminLock()` وهي تقرأ `p17b_admin_session_lock_token` من sessionStorage (تبويب-معزول) وتستدعي نفس `refresh_admin_session_lock`.

---

### 1.2 "Semester" — **صفة/عمود على subjects، وليس مستوى هرميًا مستقلًا**

**ما قالته الوثيقة:**
- `E.2` (سطر 63) و`AA.1` إلخ: "university → faculty → year → **semester** → subject".
- سطر 300: "7 levels worst case" مع عدّ semester كطبقة.
- سطر 170: "University/faculty/year/semester **entity pages**".
- Z.6 (سطر 889-892): "semester.html = pick semester … semester **cards** … → subject".
- السايت ماب (سطر 324): `semester.html Semesters of a year`.

**ما يفعله الكود فعليًا (الدليل):**

1. **لا يوجد جدول `semesters`.** الفصل تمت إضافته كعمود في `subjects` فقط:
   ```sql
   -- sql/phase4b_subject_semester.sql:27-29
   alter table subjects
     add column if not exists semester text
     check (semester in ('first', 'second', 'summer'));
   ```
   مع تعليق صريح (سطر 4-7): "لا تغيير على هرمية universities → faculties → years → subjects → resources". العمود NULLABLE، وNULL = "غير محدد/قديم".

2. **`semester.html` لا يجلب "semester" بمعرّف خاص به إطلاقًا** — يجلب السنة وكل موادها ثم يفصل **من جهة العميل**:
   ```js
   // semester.html:86-87 و 118-123 و 247-250
   const yearId = getQueryParam("year");            // المُعرّف هو YEAR، ليس semester
   const { data: subjects } = await supabaseClient.from("subjects")
     .select("id, name, code, semester").eq("year_id", yearId)...
   // التصفية محلية (client-side):
   const filtered = allSubjects.filter((s) => {
     if (currentSemesterKey === "unspecified") return !s.semester;
     return s.semester === currentSemesterKey;
   });
   ```
   و`?semester=` في الرابط هي مفتاح نصي (`first`/`second`/`summer`/`unspecified`)، **ليست id**. التبويبات تُبنى من `SUBJECT_SEMESTER_ORDER` في `js/app.js` (قيم ثابتة، لا استعلام).

3. **لوحة التحكم لا تدير الفصول ككيان CRUD مستقل.** تبويبات الأدمن العشرة (`admin/index.html:70-79`): dashboard, universities, faculties, years, **subjects**, resources, courses, reports, forum, users — **لا يوجد تبويب semesters**. الفصل مجرد `<select id="subj-semester">` داخل نموذج المادة (`admin/index.html:235`)، ويُحفظ كقيمة عمود (`admin/admin.js:1358-1370`).

4. `faculty.html:134` يمرر `semester.html?year=${y.id}` — السياق هو السنة دومًا.

**التصحيح المُقترح لوصف الوثيقة:**
- الهرمية الفعلية: **University → Faculty → Year → Subject → Resource** فقط 5 مستويات بيانات (home→…→resource = **6 مستويات أسوأ حالة**، لا 7). "Semester" **صفة تصفية (attribute/filter)** على المادة: ثلاثة أعمدة محتملة + قيمة `غير محدد`، ويُعامَل كمعاملة "قديم" في الواجهة.
- `semester.html` هي **عرض تصفية محلي لنفس السنة** (يجمّع مواد السنة تحت تبويبات فصل)، وليست عقدة كيان مستقلة؛ Z.5/Z.6 يجب دمجها: المسار الحقيقي faculty → (year) → semester.html?year= → subject.
- تصحيح Z.6: بدل "semester cards → subject"، الوصف الصحيح: "تُحمَّل السنة وكل موادها، وتُصفَّى محليًا بالتبويبات `first/second/summer/غير محدد`".
- تصحيح السايت ماب: `semester.html` تحت `years` كعرض تصفية (سطر 324)، وإزالة عدّها كطبقة في سطري 300 و1003، وعبارات E.2/AA.2 سطري 63 و970، وبSed Crumbs "up to 6 levels" (سطر 283).
- **Z.5 صحيح كما هو**: `year.html` بالفعل صفحة توجيه مؤقتة (`year.html:76-79` يفعل `window.location.replace("semester.html?year=...")`).

---

### 1.3 شارة "موثّق/منشور" — التباين الفعلي: **FAIL** — **3.01:1**

**Kالقيم الفعلية في الكود:**
```css
/* css/style.css:24,25,34,35 */
--success-bg: #E4F5EA;
--success-border: #BFE6CC;
--type-questions: #2E9E5B;
--type-questions-bg: #E4F5EA;   /* نفس خلفية success! */
```
```css
/* css/style.css:916-920 */
.tag-verified {
  background: var(--success-bg);
  color: var(--type-questions);
  border: 1px solid var(--success-border);
}
/* css/style.css:1444 */
.status-badge.published { background: var(--success-bg); color: var(--type-questions); }
```
- حجم النص في الشارتين صغير (`.status-badge` = `.76rem` bold؛ وفي جدول الأدمن `.7rem` inline عبر `admin/admin.js:1489`) → نص عادي، يلزمه **4.5:1 (AA)**.

**النتيجة المحسوبة رقميًا (WCAG formula):**
```
#2E9E5B on #E4F5EA = 3.01 : 1  →  FAIL  (تحت 4.5:1)
```
- **وبنفس الطريقة، شارة النوع "أسئلة" تفشل أيضًا:** `.tag[data-type="questions"]` (`style.css:904`) تستخدم `--type-questions-bg` (=`#E4F5EA`) مع `--type-questions` (=`#2E9E5B`) → **3.01:1 FAIL**.
- أي أن الشارتين (نوع "أسئلة" + حالة "موثّق/منشور") **تتقاسمان حاليًا نفس القيمة حرفيًا** (نفس الخلفية ونفس النص)، وأي تلميح بصري "منشور = أسئلة" موجود فعليًا في الشيفرة.

---

## الجزء 2 — حلول مقترحة لإصلاح تباين شارة موثّق/منشور (بلا تنفيذ)

**قيود القرار** (حسب `DESIGN_SYSTEM.md` §12 والتكليف): عمود نص صغير → يجب ≥ 4.5:1؛ الأولوية للون موجود (`--primary`, `--primary-dark`, `--secondary`, `--accent`) أو تغميق `--type-questions` نفسه؛ بأقصى Hue جديد واحد فقط إذا لم ينفع أي موجود؛ والتوضيح: مشتركة أم درجتان من نفس العائلة.

**المرشّحات المحسوبة على الخلفية الفعلية `#E4F5EA`:**

| المرشّح | النسبة | الحكم | ملاحظة التداخل الدلالي |
|---|---|---|---|
| `--type-questions #2E9E5B` (الحالي) | **3.01:1** | **FAIL** | القيمة الحالية |
| `--accent #E9A426` | 1.89:1 | FAIL | تم استبعاده (وهو لأغراض تركيز/توهج لا نص) |
| `--accent-focus #B45309` | 4.44:1 | FAIL (أقل حاف) | محجوز للـ focus ring |
| `--secondary #1F7A8C` | 4.39:1 | FAIL | قريب لكن تحت الحد؛ يتداخل مع نوع slides تمامًا (نفس الهيكس!) |
| `--type-summary #0F766E` | 4.83:1 | PASS | **لكنه لون نوع summary** — استخدامه لشارة "موثّق" يجعلها تُباع taste مع وسوم الملخصات ← مرفوض |
| `--type-lecture #0E7490` | 4.73:1 | PASS | مرفوض لنفس السبب (لون نوع lecture حصري) |
| `--primary #0F4C5C` | 8.40:1 | PASS | تيل-حبر العلامة؛ لا يطابق أي لون نوع |
| `--primary-dark #0A3A47` | 10.85:1 | PASS | أقصى هامش؛ لا يطابق أي لون نوع |
| تغميق نفس عائلة الأخضر: `#147D45` | 4.58:1 | PASS | يحافظ على فرق الدرجة عن summary الداكن |
| تغميق نفس العائلة: `#166534` | 6.30:1 | PASS | متوازن |
| تغميق نفس العائلة: `#14532D` | 8.05:1 | PASS | قريب من لون الملخصات القديم |

---

### الحل 1 — المشتركة الفعلية: تغميق `--type-questions` نفسه (قيمة واحدة للشارتين — Option A)

- **الخطة:** تغيير قيمة `--type-questions` من `#2E9E5B` إلى `#166534` (أو الحد الأدنى `#147D45`). يلتقط ذلك تلقائيًا: `.tag-verified` و`.status-badge.published` **و** شارة النوع `questions` معًا (كلها تستمد من نفس الرمزين).
- **التباين:** `#166534` on `#E4F5EA` = **6.30:1 PASS** (الحد الأدنى `#147D45` = 4.58:1 PASS).
- **التداخل الدلالي:** الشارتان تظلان **بقيمة واحدة مشتركة** (كما اليوم)؛ لون "أسئلة" يغيّر قيمته إلى أخضر أعمق، و"موثّق" يرثه — من نفسfamily الأخضر، متمايز عن `summary #0F766E` (تيل).
- **إيجابي:** تغيير رمز واحد، صفر Hue جديد، يصلّح وسوم "أسئلة" المعطوبة أيضًا.
- **سلبي:** يبقى التميّز البصري بين "نوع: أسئلة" و"حالة: موثّق" غائبًا (متطابقتان)، وهو ذات ملاحظة بصرية في الوثيقة (V1: "reuses type-questions green … formalize for clarity").

### الحل 2 — الفصل إلى درجتين من نفس العائلة (Option B — الموصى به)

- **الخطة:** رمز جاهز فعليًا يقسم الدور:
  - `--type-questions` → `#147D45` (4.58:1 PASS) يبقى لشارة النوع "أسئلة".
  - شارة الحالة (`.tag-verified`, `.status-badge.published`) → رمز مخصص جديد داخل family الأخضر **`#166534`** (6.30:1 PASS) — سكرين شوت خارج V1's "success" notation.
- **التباين:** `#166534` on `#E4F5EA` = **6.30:1 PASS**؛ `#147D45` = 4.58:1 PASS.
- **التداخل الدلالي:** درجتان لونيتان من نفس العائلة (أخضر): "أسئلة" أفتح، "موثّق" أعمق/أقوى — لا تطابق مع أي لون نوع آخر؛ يزيل اللبس البصري الكائن وتتوافق مع نية V1 ("formalize the success token").
- **إيجابي:** يميّز الدلالة (نوع vs حالة) بصريًا + يصلّح التباين؛ صفر Hue جديد.
- **ملاحظة حوكمة:** لا يضيف هوية لونية جديدة (درجتان من عائلة موجودة) — يتوافق مع §12 دون مستند قرار.

### الحل 3 — الحبر-الأصلي (أقصى هامش، صفر رموز جديدة — Option C)

- **الخطة:** شارة الحالة → `--primary-dark #0A3A47` (10.85:1) أو `--primary #0F4C5C` (8.40:1) على الخلفية الخضراء التيل. ولأن شارة النوع "أسئلة" تعطّل أيضًا بمفردها، تُغلّمق `--type-questions` إلى `#166534` (6.30:1).
- **التباين:** `#0A3A47` on `#E4F5EA` = **10.85:1 PASS** (أو `#0F4C5C` = 8.40:1).
- **التداخل الدلالي:** تيل-حبر العلامة لا يطابق أي لون نوع واحد بالضبط؛ يمنح مظهر "سلطة/نشر" أقوى. **مقايضة:** خسارة دلالة "الأخضر = نجاح/موثّق" (لون-only)، تُعوَّض جزئيًا بعلامة ✓ وبالخلفية `--success-bg` والحدود. يصلّح شارة "أسئلة" أيضًا عبر تغميق `--type-questions`.

---

## الملخص التنفيذي

1. **Session Lock (Z.14):** الادعاء "informative, not blocking (RLS is enforcer)" **خاطئ**. الحقيقة: **blocking** — دخول/استعادة فاشل = `signOut()` + شاشة دخول فورًا؛ فقدان القفل عبر heartbeat (25s) = `forceLockLogout()`؛ الفرض حصريًا من القاعدة عبر `acquire/refresh/release_admin_session_lock` (SECURITY DEFINER) + deny-all RLS + TTL 90s، والواجهة مجرد مرآة.
2. **Semester:** **صفة/عمود `subjects.semester`** (nullable، 3 قيم + null)، **لا مستوى ولا جدول `semesters` ولا كيان CRUD**. `semester.html?year=` يعرض مواد سنة واحدة ويصفّيها محليًا. الهرمية الحقيقية: University → Faculty → Year → Subject → Resource (6 مستويات أسوأ حالة). يجب تصحيح E.2/170/300/324/1003/1118 وZ.6 والسايت ماب وبSed crumbs.
3. **شارة موثّق/منشور:** القيمة الفعلية `#2E9E5B` على `#E4F5EA` = **3.01:1 → FAIL** (نص صغير، حد 4.5:1). شارة النوع "أسئلة" معطوبة بنفس النسبة (نفس الرمزين تمامًا).
4. **الحلول المقترحة (بدون تنفيذ):** A) تغميق `--type-questions` → `#166534` (6.30:1) بإبقاء الاشتراك؛ B) تقسيم لعائلة خضراء: أسئلة `#147D45` (4.58:1) + موثّق `#166534` (6.30:1) ← **موصى به**؛ C) `--primary-dark #0A3A47` (10.85:1) للشارة مع تغميق `#type-questions#166534`.

**القراءة المفتوحة:** لم تُجرَّ أي تعديلات على شيفرة أو وثائق تصميم في هذا التقرير.