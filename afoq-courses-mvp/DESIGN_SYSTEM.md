# نظام التصميم — أفق المعرفة

> مرجع الهوية البصرية الرسمي للمشروع. يوثّق ما هو **مطبَّق فعليًا** في `css/style.css` فقط — لا يقترح ولا يخترع أي قيمة غير موجودة في الكود.

---

## 1. Brand Identity

**اسم المنصة:** أفق المعرفة

**الوصف:** منصة أكاديمية طبية للطلاب، هدفها تنظيم والوصول إلى الموارد التعليمية الطبية بطريقة سريعة وموثوقة ومريحة للدراسة.

**فلسفة التصميم:** Medical + Academic + Modern + Calm + Trustworthy + Student-friendly

هذه الفلسفة تعني عمليًا:
- **Medical / Academic:** لون أساسي teal هادئ (`--primary`) بدل الأزرق الشائع في منصات التعليم العربية، لإحساس مهني مميّز.
- **Modern:** بطاقات نظيفة، ظلال خفيفة جدًا (`--shadow`)، بلا زخرفة زائدة.
- **Calm:** خلفية شبه بيضاء دافئة (`--bg: #F9FBFC`) بدل الأبيض الكامل، لتقليل إجهاد العين في جلسات الدراسة الطويلة.
- **Trustworthy:** تباين ألوان محسوب، تسلسل بصري واضح، بلا ألوان صاخبة.
- **Student-friendly:** خط عربي مريح (Tajawal) للنص، ومساحات كافية حول العناصر القابلة للنقر (44px كحد أدنى).

---

## 2. Color System

كل القيم التالية منسوخة حرفيًا من `:root` في `css/style.css`.

### الهوية الأساسية

| الدور | Token | القيمة |
|---|---|---|
| Primary | `--primary` | `#0F4C5C` |
| Primary Hover | `--primary-dark` | `#0A3A47` |
| Secondary | `--secondary` | `#1F7A8C` |
| Accent | `--accent` | `#E9A426` |
| Inverse | `--inverse` | `#0F2A33` |

### الخلفيات

| الدور | Token | القيمة |
|---|---|---|
| Background (خلفية الصفحة) | `--bg` | `#F9FBFC` |
| Surface (بطاقات/مكوّنات) | `--surface` | `#FFFFFF` |
| Muted (خلفيات غاطسة: حقول إدخال، hover، رقائق) | `--muted-bg` | `#EEF4F6` |

### النصوص

| الدور | Token | القيمة |
|---|---|---|
| Primary text | `--ink` | `#0F172A` |
| Secondary text | `--ink-soft` | `#475569` |
| Tertiary text | `--ink-faint` | `#94A3B8` ⚠️ راجع قسم [10. Accessibility](#10-accessibility) |

### الحدود

| الدور | Token | القيمة |
|---|---|---|
| Border | `--line` | `#E2E8F0` |
| Border (أقوى) | `--line-strong` | `#CBD5E1` |

### حالات الدلالة (Semantic)

| الدور | Token | القيمة |
|---|---|---|
| Danger | `--danger` | `#DC2626` |
| Danger background | `--danger-bg` | `#FEF2F2` |
| Danger border | `--danger-border` | `#FECACA` |
| Success background | `--success-bg` | `#E4F5EA` |
| Success border | `--success-border` | `#BFE6CC` |
| Neutral background (شارات محايدة) | `--neutral-bg` | `#F1F1F1` |
| Focus ring | `--focus` | `var(--accent)` → `#E9A426` |

> لا يوجد Token باسم `--success` (لون نص) منفصل في الكود؛ نص حالة "منشور/موثّق" يستخدم `--type-questions` (انظر قسم أنواع الموارد). لا يوجد Token باسم `--warning` في الكود حاليًا.

### قياسات مشتركة (غير ألوان لكن جزء من الهوية)

| Token | القيمة | الاستخدام |
|---|---|---|
| `--radius` | `14px` | نصف قطر البطاقات والعناصر الكبيرة |
| `--radius-sm` | `8px` | نصف قطر العناصر الصغيرة (أيقونات، حقول) |
| `--shadow` | `0 2px 10px rgba(18,38,42,.06)` | ظل افتراضي للبطاقات |
| `--shadow-hover` | `0 8px 24px rgba(18,38,42,.12)` | ظل عند hover/تركيز |

---

## 3. Color Usage Rules

**Primary (`--primary` / `--primary-dark`)**
- خلفية الهيدر (site-header)
- الأزرار الأساسية (`.btn-primary`)
- العناوين (h1–h4 تستخدم `--primary-dark`)
- حدود/نص الزر الثانوي (`.btn-outline`)
- الحالة النشطة في تبويبات الأدمن (`.admin-tab-btn.active`)

**Secondary (`--secondary`)**
- روابط الفوتر (`.footer-links a`)
- لون شارة نوع "سلايدات" (مرتبط بالهوية كما هو موثّق في نظام أنواع الموارد)

**Accent (`--accent`)**
- حلقة التركيز (`:focus-visible`) في كل الموقع
- الشريط الذهبي أسفل صناديق البحث الكبيرة (search-hero / home-hero / landing-hero)
- نقطة "eyebrow" الزخرفية، نقطة القلب المفضّل عند التفعيل (`.favorite-btn.active`)
- **يُستخدم باعتدال فقط** — لا يُستخدم كخلفية عنصر كبير أو كلون نص أساسي؛ الهدف إبقاؤه مميزًا ولافتًا عند ظهوره (تركيز لوحة المفاتيح، تنبيه صغير، عنصر واحد بارز في الصفحة).

**Background / Surface / Muted**
- `--bg`: خلفية `<body>` فقط.
- `--surface`: كل البطاقات، الهيدر الفرعي، الفوتر، النوافذ المنبثقة.
- `--muted-bg`: الأسطح "الغاطسة" داخل بطاقة أو صفحة — حقول الإدخال، hover لعناصر القائمة، خلفية شارة اللغة، خلفية بطاقات الإحصائيات في لوحة التحكم.

**النصوص**
- `--ink`: النص الأساسي وعناوين البطاقات.
- `--ink-soft`: نص ثانوي/وصفي (metadata، وصف تحت العنوان، تلميحات).
- `--ink-faint`: محجوز لنص كبير/عريض فقط بسبب التباين المنخفض (راجع قسم Accessibility) — غير مستخدم حاليًا على نص محتوى عادي.

**Danger**
- رسائل الخطأ في نماذج الأدمن (`role="alert"`)، زر الحذف (`.btn-danger`)، شارة "مُبلَّغ عنه" (`.status-badge.reported`)، hover لزر الإبلاغ عن مورد.

---

## 4. Resource Types

نمط شارات أنواع الموارد: خلفية فاتحة (`-bg`) + نص/حدّ بلون داكن من نفس العائلة اللونية، لضمان تباين مقروء (مطبَّق في `.tag[data-type]`, وأيضًا لون الشريط الجانبي لبطاقة المورد `.resource-card[data-type]` ونقطة `.landing-type-dot`).

| النوع | Text/Border Token | القيمة | Background Token | القيمة | الاستخدام |
|---|---|---|---|---|---|
| محاضرة (lecture) | `--type-lecture` | `#0E7490` | `--type-lecture-bg` | `#E0F2F7` | أزرق-سماوي بارد، للتمييز عن باقي الأنواع |
| سلايدات (slides) | `--type-slides` | `#1F7A8C` | `--type-slides-bg` | `#E6F0F3` | نفس لون Secondary — مرتبط بالهوية |
| كتاب (book) | `--type-book` | `#5B21B6` | `--type-book-bg` | `#EDE9FE` | بنفسجي، للمراجع الكاملة |
| ملخص (summary) | `--type-summary` | `#0F766E` | `--type-summary-bg` | `#CCFBF1` | أخضر-تيال، للمراجعة السريعة |
| أسئلة (questions) | `--type-questions` | `#2E9E5B` | `--type-questions-bg` | `#E4F5EA` | أخضر، نفس اللون يُستخدم أيضًا لشارة "موثّق/منشور" (`.tag-verified`, `.status-badge.published`) |
| ملاحظات (notes) | `--type-notes` | `#92400E` | `--type-notes-bg` | `#FEF3C7` | عنبر دافئ، لمحتوى شخصي/غير رسمي |
| أسئلة سابقة (past_exam) | `--type-past_exam` | `#9D174D` | `--type-past_exam-bg` | `#FCE7F3` | وردي تنبيهي، لأهمية الامتحانات السابقة |

> ملاحظة: نوع "أسئلة" (questions) موجود في الكود كنوع مورد مستقل عن "أسئلة سابقة" (past_exam)، ويتشارك لونه مع دلالة "موثّق" لأنه غير مذكور في طلب تغيير الهوية الأخير فبقي على لونه الأصلي.

---

## 5. Typography

**الخطوط الفعلية (عبر Google Fonts):**
- **Cairo** (600/700/800) — للعناوين
- **Tajawal** (400/500/700) — لبقية النصوص، وهو خط الجسم الافتراضي لـ `<body>`

| العنصر | Font | Weight | حجم فعلي | لون |
|---|---|---|---|---|
| H1 (في القاعدة العامة h1–h4) | Cairo | 800 | يعتمد على السياق — لا يوجد حجم عام موحّد؛ العناوين الكبيرة داخل الأقسام البطلية تستخدم `clamp()` مثال: `.home-hero h1` بحجم `clamp(1.9rem, 1.15rem + 3.2vw, 3.1rem)`، و`.landing-hero-title` بحجم `clamp(1.85rem, 1.2rem + 2.8vw, 2.9rem)`، و`.search-hero h1` بحجم `clamp(1.4rem, 3vw, 2rem)` | `--primary-dark` (أبيض `#fff` فوق خلفية `--primary` في الأقسام البطلية) |
| H2 / H3 | Cairo | 800 | حجم المتصفح الافتراضي للعنصر ما لم يُحدَّد صراحة (مثال: `.card h3` بحجم `1.05rem`) | `--primary-dark` |
| Body | Tajawal | 400 (الافتراضي)، حتى 700 عند التأكيد | `1rem` تقريبًا، `line-height: 1.6` على مستوى `<body>` | `--ink` |
| Metadata (وصف، تلميحات، تواريخ) | Tajawal | 400–700 حسب السياق | غالبًا `.8rem`–`.88rem` (مثال: `.card-sub` بحجم `.85rem`) | `--ink-soft` |
| Buttons | Tajawal | 700 | `.95rem` (`.btn`)، أو `.82rem` للحجم الصغير (`.btn-sm`) | حسب نوع الزر |

> لا تُستخدم أي خطوط أخرى غير Cairo وTajawal في المشروع حاليًا.

---

## 6. Components

**Header**
- خلفية `--primary`، نص أبيض، `position: sticky`، ظل خفيف (`--shadow`).
- الشعار داخل حاوية بيضاء صغيرة الحواف لتبرز فوق الخلفية الداكنة.
- أزرار الأيقونات شبه شفافة (`rgba(255,255,255,.15)`) تتكثّف عند hover.

**Navigation**
- روابط على شكل "حبة" (pill) بخلفية شبه شفافة فوق الهيدر، تتكثّف عند hover/focus.
- على الشاشات الصغيرة جدًا تختفي النصوص وتبقى الأيقونات فقط (مع `aria-label` محفوظ).

**Breadcrumbs**
- كل خطوة سابقة: حبة بخلفية `--surface` وحدّ `--line`، تتحول إلى `--primary` عند hover.
- الصفحة الحالية: نص عريض بلون `--primary-dark` بلا خلفية حبة — تمييز واضح عن باقي المسار.

**Search**
- حقل داخل كبسولة بيضاء بارزة (`--surface` + `--shadow`)، حدود شفافة تندمج مع الكبسولة داخل الأقسام البطلية، وتتحول إلى `--primary` عند focus.
- حلقة تركيز صريحة بلون `--focus` عبر قاعدة `:focus-visible` مخصّصة لضمان ظهورها فوق قاعدة `:focus` العامة.

**Cards**
- خلفية `--surface`، حدّ `--line`، `--radius`، ظل `--shadow` يتحول إلى `--shadow-hover` مع ارتفاع خفيف (`translateY(-2px)`) عند hover/focus.

**Resource Cards**
- نفس أساس البطاقة، مع شريط جانبي ملوّن حسب النوع (`border-inline-start: 6px solid var(--type-x)`).
- شارات النوع/اللغة/التوثيق أعلى البطاقة، إجراءات (تحميل، مفضلة، إبلاغ) أسفلها بعد خط فاصل `--line`.

**Badges (Tags)**
- شكل حبة صغيرة، خلفية فاتحة من نفس عائلة لون النوع + نص/حدّ داكن من نفس العائلة (راجع قسم أنواع الموارد).
- شارات الحالة في لوحة التحكم (`.status-badge`) تتبع نفس المنطق: `--success-bg`/`--neutral-bg`/`--danger-bg` مع نص مطابق.

**Buttons**
- Primary: خلفية `--primary`، تغميق إلى `--primary-dark` عند hover.
- Outline/Secondary: حدّ وخلفية شفافة بلون `--primary`، خلفية `--muted-bg` عند hover.
- Danger: خلفية `--danger` بحدّ مطابق.
- كلها بشكل حبة كاملة الاستدارة (`border-radius: 999px`)، حد أدنى للارتفاع 44px في الأزرار العادية.

**Forms**
- تسميات عريضة صغيرة، حقول بخلفية `--muted-bg` (أو `--surface` حسب السياق) وحدّ `--line-strong`، تتحول إلى حدّ `--primary` عند focus.
- رسائل الخطأ نص بلون `--danger` مع `role="alert"`.

**Footer**
- خلفية `--surface`، حدّ علوي `--line`، نص وصفي بلون `--ink-soft`، روابط بلون `--secondary` (تتحول إلى `--primary` عند hover)، سطر الحقوق السفلي بلون `--ink-soft`.

**Tables (لوحة التحكم)**
- حدود سفلية خفيفة `--line` بين الصفوف، رؤوس بلون `--ink-soft`، hover خفيف بخلفية `--muted-bg`.
- على الشاشات الصغيرة تتحول كل صف إلى بطاقة قابلة للطي (بدل جدول أفقي).

**Toasts**
- خلفية `--inverse` (سطح داكن معكوس)، نص أبيض، شكل حبة، ظل `--shadow-hover`.

**Empty / Loading States**
- `.state-msg`: نص بلون `--ink-soft`، خلفية `--surface`، حدّ متقطّع (`dashed`) بلون `--line-strong` — إشارة بصرية هادئة لحالة "لا يوجد محتوى بعد" بدون استخدام ألوان تحذيرية.

---

## 7. Future Feature — Courses

> هذا قسم توثيقي فقط، لا ينفّذ أي شيء حاليًا.

عند تطوير ميزة الدورات مستقبلًا، يجب الالتزام بما يلي:
- خلفية بطاقة الدورة = `--surface` (نفس بطاقات الموارد).
- المعلومات الأساسية (اسم الدورة، الجهة، الحالة) = درجات `--primary` / `--ink`.
- تصنيف الدورة (Course category) = `--secondary`، بنفس منطق شارات أنواع الموارد الحالية (خلفية فاتحة + نص داكن من نفس العائلة).
- الإجراء الرئيسي (Enroll/CTA) = `--accent` عند الحاجة فقط لعمل قابل لالتصاق نظر الطالب، وليس كخلفية دائمة لكل الأزرار.
- **لا** يُستحدث أي لون Brand جديد خارج الجدول الموثّق أعلاه.
- **لا** تُستخدم gradients مبالغ فيها أو أي أسلوب بصري غير موجود حاليًا في `css/style.css`.
- يجب أن تبدو الدورات جزءًا طبيعيًا من نفس منصة أفق المعرفة، وليست منتجًا أو منصة منفصلة بصريًا.

---

## 8. Future Feature — Student Community

> هذا قسم توثيقي فقط، لا ينفّذ أي شيء حاليًا.

- نفس الهوية البصرية الحالية بالكامل — بلا استثناء ألوان جديدة.
- `--secondary` و`--muted-bg` مناسبان لتصنيف المواضيع/الأقسام والخلفيات الغاطسة (صناديق النقاش، الفلاتر).
- `--accent` يُستخدم فقط للتفاعلات المهمة القليلة: إشعار جديد، علامة "جديد" على موضوع.
- التصميم يجب أن يكون اجتماعيًا ودافئًا ضمن نفس الهدوء البصري العام — بلا تحويل المنصة إلى نسخة من شبكة اجتماعية (بلا ألوان صاخبة أو تفاعلات مبالغ فيها).
- يبقى الطابع أكاديميًا ومحترمًا، متوافقًا مع فلسفة Medical + Academic + Trustworthy.

---

## 9. About Us — Future Direction

> هذا قسم توثيقي فقط، لا ينفّذ أي شيء حاليًا.

- نفس الهوية البصرية، نفس نظام الألوان، نفس الخطوط (Cairo/Tajawal) — بلا تصميم منفصل عن بقية المنصة.
- التركيز في المحتوى والتصميم على: الثقة (Trustworthy)، الهدف من المنصة، الطلاب، وجودة المحتوى الأكاديمي — لا على عناصر بصرية زخرفية إضافية.

---

## 10. Accessibility

- **التباين (WCAG-conscious contrast):** كل أزواج النص/الخلفية الأساسية في النظام تحقق 4.5:1 على الأقل للنص العادي (مثال: `--ink` على `--bg` ≈ 17:1، `--ink-soft` على `--bg` ≈ 7.3:1).
- **⚠️ لا تُستخدم `#94A3B8` (`--ink-faint`) كنص body صغير على خلفية بيضاء/فاتحة** — تباينها الفعلي ≈ 2.56:1 على أبيض، وهو أقل من الحد الأدنى لـ WCAG AA للنص العادي. الاستخدام الآمن الوحيد لهذا اللون هو نص كبير/عريض جدًا (18.7px عريض فأكثر تقريبًا)، وحاليًا **غير مُستخدم على أي نص محتوى في الكود**.
- **`:focus-visible`:** حلقة تركيز موحّدة بلون `--focus` (`3px solid`, `outline-offset: 2px`) على كل عنصر قابل للتركيز في الموقع، بما فيها استثناءات مقصودة لحقول بحث معينة موثّقة داخل CSS نفسه بتعليقات صريحة.
- **Keyboard accessibility:** لا يوجد أي `outline: none` بدون حلقة تركيز بديلة صريحة في نفس القاعدة أو قاعدة لاحقة.
- **Reduced motion:** قاعدة `@media (prefers-reduced-motion: reduce)` تعطّل كل الحركات والانتقالات وسلوك `scroll-behavior: smooth`.
- **RTL:** الموقع بالكامل `direction: rtl` مع استخدام خصائص CSS المنطقية (`inset-inline-start`, `border-inline-start`, `padding-inline-end`... إلخ) بدل `left/right` الثابتة.
- **Touch targets:** حد أدنى 44×44px للعناصر التفاعلية الرئيسية (الأزرار، أيقونات الهيدر، زر المفضلة، الروابط السريعة في الصفحة الرئيسية).

---

## 11. Responsive Design

يجب أن يستمر التصميم بالعمل دون كسر عند: **320px, 375px, 768px, 1024px, 1440px**، وذلك عبر:
- شبكات مرنة (`grid-template-columns: repeat(auto-fill, minmax(...))`) بدل أعمدة ثابتة العدد.
- نقاط توقف فعلية موجودة في الكود عند: `480px, 560px, 640px, 720px, 900px, 960px`.
- تحويل جداول لوحة التحكم إلى بطاقات قابلة للطي تحت `640px` بدل تمرير أفقي.
- عدم السماح بأي `overflow` أفقي، بطاقات مكسورة، نص مقصوص، أو أزرار غير قابلة للاستخدام على أي من المقاسات أعلاه.

---

## 12. Design Principles

1. لا تُضاف أي لون Brand جديد بدون سبب موثَّق في هذا الملف.
2. لا تتغيّر الهوية البصرية لمجرد إضافة Feature جديدة.
3. `--accent` يُستخدم باعتدال — يبقى مميزًا لأنه نادر.
4. لا تُستخدم gradients إلا إذا أُضيفت رسميًا لهذا النظام لاحقًا.
5. لا تُستخدم ألوان neon.
6. لا يُستخدم Glassmorphism.
7. لا يجب أن يبدو أي جزء من المنصة كقالب (Template) جاهز عام.
8. كل Feature جديدة يجب أن تبدو جزءًا طبيعيًا من أفق المعرفة، لا منتجًا منفصلاً.
9. Accessibility أهمّ من أي اعتبار جمالي بحت.
10. البساطة وسرعة الوصول للمعلومة أهمّ من الزخرفة البصرية.

---

## 13. Developer Rules

كل مطوّر يعمل مستقبلًا على المشروع يجب أن:
- يستخدم CSS variables الموجودة في `:root` بدل كتابة قيم HEX مباشرة.
- لا يكرر قيم HEX عشوائيًا في أماكن متعددة — إن تكرر لون، يُستخرج كـ Token جديد بدل تكراره.
- لا يُنشئ لونًا جديدًا إذا كان هناك Token موجود يغطي نفس الحاجة.
- يحافظ على سلوك RTL في أي عنصر جديد (خصائص منطقية لا اتجاهية).
- يحافظ على حالات `:focus-visible` الموجودة ولا يزيلها أو يستبدلها بـ `outline: none` بدون بديل.
- يحافظ على السلوك المتجاوب (Responsive) عند أي مقاس مذكور في القسم 11.
- **لا يُعدّل أي functionality أثناء مهمة تصميم بصري**، إلا إذا طُلب ذلك صراحة في المهمة نفسها.

---

## 14. Design System Source of Truth

- **`DESIGN_SYSTEM.md`** = المرجع التوثيقي للهوية البصرية — يُقرأ للفهم والتخطيط.
- **`css/style.css`** = المصدر الفعلي الوحيد لتطبيق الهوية داخل المشروع — هو ما يُنفَّذ فعليًا في المتصفح.

> عند وجود أي اختلاف بين هذا التوثيق والكود الفعلي: **يجب مراجعة الكود الفعلي (`css/style.css`) أولًا، ولا يُفترض أن التوثيق أحدث من التطبيق.** هذا الملف يجب تحديثه ليطابق الكود عند اكتشاف أي فرق، لا العكس.
